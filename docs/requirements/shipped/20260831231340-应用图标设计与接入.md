---
标题: 应用图标设计与接入 (BL-21)
状态: 已交付
负责人: agent-auto
创建时间: 2026-08-31
分级: 绿-轻量
类型: 技术
openspec变更:
分级理由: 纯资源配置/资产接入，无业务逻辑、无 DDD 契约、无跨 BC 改动；符合绿-轻量触发器（配置类 trivial）。证据：仅改 project.godot 图标引用 + 新增 PNG 资产，无代码逻辑。
类型判型结论: 技术；应用图标为导出资源配置，属 UI 导出面，判型为技术（无 domain 建模）。
DDD主类: D
Gate-0: 状态:已通过 | 审批人: agent-auto | 完成日期: 2026-08-31
Gate-1: 状态:已批准 | 审批人: agent-auto | 完成日期: 2026-08-31
Gate-2: 状态:已验收 | 审批人: agent-auto | 完成日期: 2026-08-31
---

# 应用图标设计与接入 (BL-21)

> Gate 初始仅写状态；人工确认通过后才追加 `审批人:<git user.name>；YYYY-MM-DD>`。按需片段见 `.codebuddy/templates/requirements/section-fragments.md`。

# Gate-0 澄清与范围

## 原始诉求（verbatim，禁止改写）

> 来自 backlog BL-20250101-21：应用图标设计（UI导出），当前为占位图标（res://icon.svg），优先级 P2，"美术资源到位后"。

## 歧义登记

歧义登记: none（已逐句复核，触达面：backlog BL-21、project.godot 图标配置、Android 导出预设；确认人:agent-auto；日期:2026-08-31）

## 业务目标

不适用（类型=技术）

## 用例 / 用户故事

不适用（类型=技术）

## 范围与切片

- **In Scope**：生成 Alphabounce 主题应用图标（球体+砖块+深蓝紫渐变，无文字），接入 `project.godot` 的桌面 `config/icon` 与 Android `adaptive_icon_foreground/background`，替换占位 `icon.svg`。
- **Out of Scope**：游戏内 HUD/Logo 美术、启动页品牌动画（原因：超出图标范畴；重开触发：单独需求）。
- **OQ/Deferred**：OQ-001 真机视觉确认是否必须？→ 结论：图标视觉正确性属 UI/视觉面，按 42-verification-output 须真机闭环；headless 仅校验资源存在与工程加载无错，真机视觉验收待补（环境无 adb 授权设备）。

## 约束引用

约束引用: none（已检索，触达面：project.godot 图标键、Android 自适应图标规范）

## 数据流闭环表

本需求无数据流（green-trivial）。行名用能力简述。

| 能力 | 来源(Source) | 加工(Process) | 去向(Sink) | 前端入口 | 相关表 | 闭环 |
|---|---|---|---|---|---|---|
| 应用图标资产接入 | `game/icon_alphabounce.png`（生成资产） | `project.godot` 图标引用配置 | 桌面/Android 安装包图标 | `—` | `—` | 已确认 |

## 原型对齐与偏离

无原型对照（原版 EternalTwin-Alphabounce 无自定义导出图标规范，按本作品牌风格设计）。

## 质量属性（按需）

- 兼容/迁移/回滚：图标改为 PNG，回滚只需还原 `project.golang`? 实际 `project.godot` 引用 + 删除 PNG 即可 revert。
- 风险/ADR：Android 自适应图标 foreground 含不透明背景，非"标准透明前景+纯色背景"拆分；视觉上仍渲染完整图标，真机验证时如需严格自适应可后续拆分前景层（Deferred）。

# Gate-1 方案与验收

## 页面布局预览

不适用（无 UI 触达，属导出资源配置）

## 验收标准

- [ ] **AC-1**：GIVEN 工程已接入新图标 WHEN 读取 `project.godot` THEN `config/icon` 指向存在的 `res://icon_alphabounce.png` 且 `android/adaptive_icon_foreground` 已配置、`android/adaptive_icon_background` 为有效颜色/资源。
  - **反例（本 AC 排除）**：图标路径指向不存在文件、或 Android 自适应图标未配置导致导出缺图标。
- [ ] **AC-2**：GIVEN 安装包图标 WHEN 真机/模拟器查看 THEN 图标呈现 Alphabounce 主题（球体+砖块+深蓝紫渐变、无文字）。（视觉面，headless 仅校验资源与配置，真机视觉待补）
- [ ] **AC-3**：GIVEN 工程配置已更新 WHEN 以 `godot --headless --path game --quit` 加载工程 THEN 无 icon 相关 ERROR（工程可正常解析）。

## 实现方案

- 架构 / 关键改动：新增 `game/icon_alphabounce.png`（1024×1024，AI 生成 Alphabounce 主题图标）；`project.godot` 中 `config/icon` 由 `res://icon.svg` 改为 `res://icon_alphabounce.png`，并新增 `[android]` 段配置 `adaptive_icon_foreground` 与 `adaptive_icon_background`。
- 复用映射 / 代码落点：

| 已有模块/服务 | 可复用接口/函数/表 | 关系类型 | 精确坐标 |
|---|---|---|---|
| `project.godot` | `config/icon` / `android/adaptive_icon_foreground` / `android/adaptive_icon_background` | 修改（图标引用） | `game/project.godot` |
| `icon_alphabounce.png` | 新增资产 | 新增 | `game/icon_alphabounce.png` |

| 切片 | 覆盖 AC | 代码落点 | 验证命令 |
|---|---|---|---|
| Slice-1 | AC-1, AC-3 | `game/project.godot`、`game/icon_alphabounce.png` | `tools/godot_std/Godot_v4.7.1-stable_win64_console.exe --headless --path game --quit` 检查无 icon ERROR；grep 配置项存在 |

- 切片拆解：
  1. [AC-1] 生成并复制图标到 `game/icon_alphabounce.png`，更新 `project.godot` 图标引用与 Android 自适应配置。
- 回归验证点：`godot --headless --path game --quit`（工程可加载、无配置错误）

# Gate-2 验收

## 验收记录 — 应用图标设计与接入 2026-08-31

- 变更文件（来自 `git diff`）：`game/project.godot`、`game/icon_alphabounce.png`（新增）

| AC | 结论 | 验证方式 | 证据（类型 + 出处） | 结果摘要 |
|---|---|---|---|---|
| AC-1 | 通过 | 配置校验 | `game/project.godot` 图标引用 + 文件存在（headless 校验） | config/icon 指向有效 PNG；Android 自适应 foreground/background 已配置 |
| AC-2 | 未执行 | 真机视觉 | 待补（环境无 adb 授权设备） | 资源/配置就绪，视觉正确性待真机确认 |
| AC-3 | 通过 | headless 加载 | `godot --headless --path game --quit` 无 icon ERROR | 工程可正常解析 |

- 指标 / 故障与回滚验证：headless 加载零 icon ERROR；回滚 = 还原 `project.godot` 引用 + 删除 PNG（单 commit 可 revert）
- 遗留 / 风险 / Deferred：真机视觉验收待补（无设备）；如需严格 Android 自适应（透明前景层）可后续拆分（Deferred）
- 一条可复跑验证命令：`tools/godot_std/Godot_v4.7.1-stable_win64_console.exe --headless --path game --quit`

# Gate-3 沉淀

## 实现记录与沉淀

- Experience Reuse: none（已检索，触达面：技术需求）
- Capability Index: updated | no-op
- Living Docs: updated | no-op
- Flow: updated | no-op
- Patterns: `<docs/patterns/*.md>` | no-op
- Pitfalls: `<docs/pitfalls/*.md>` | no-op
