---
标题: 20250101130002-球体物理系统完整化（Godot Physics2D集成）
状态: 待处理
负责人: Agnes
创建时间: 2025-01-01
分级: 黄
类型: 技术
种子: true
openspec变更:
分级理由: ball.gd 已有基础框架但使用自定义速度追踪，需迁移到 Godot Physics2D 以保证碰撞系统兼容
类型判型结论: 技术；游戏核心物理系统重构
DDD主类: B
Gate-0: 状态:待确认
Gate-1: 状态:待批准
Gate-2: 状态:待验收
---

# 20250101130002-球体物理系统完整化（Godot Physics2D集成）

# Gate-0 澄清与范围

## 原始诉求（verbatim，禁止改写）

> 将 ball.gd 从自定义速度追踪迁移到 Godot CharacterBody2D + move_and_slide()，接入 Physics2D 碰撞系统。

## 歧义登记

歧义登记: none（已逐句复核，触达面：球体物理；确认人:Agnes；日期:2025-01-01）

## 业务目标

使球体物理与 Godot 内置碰撞系统完全兼容，确保与方块、边界、敌人的碰撞检测准确可靠。

## 用例 / 用户故事

不适用（类型=技术）

## 范围与切片

> 背景：当前 `ball.gd` 使用手动 `position += velocity * delta` 和边界 clamp，未使用 Godot Physics2D。
> In Scope 只写能力边界。

### In Scope（本期交付）

- 重构 ball.gd 使用 `CharacterBody2D.move_and_slide()` 替代手动位置更新
- 配置 CollisionShape2D 为 CircleShape2D（已在 Ball.tscn 中，radius=10）
- 配置正确的碰撞层/掩码（layer=4(ball), mask=5(block+boundary)）
- 边界反弹：使用 Godot PhysicsServer 或 CollisionShape2D 配合 Area2D 边界
- 保留原有 `launch(direction)` 接口（Pad 依赖此 API）
- 保留 `get_move_velocity()` / `is_launched_flag()` / `get_type()` 接口

### Out of Scope

- 不做特殊球体效果（火焰弹跳、冰冻减速等）（原因：后续需求；重开触发：独立 PRD）
- 不做导弹物理（原因：R09 导弹系统；重开触发：R09）
- 不做球体重力（AlphaBounce 原版无重力，纯弹跳）
- Gate-1 前不修改 Block 碰撞层（原因：依赖 R04；重开触发：R04 同时实现）

### 待确认问题与延期项（Open Questions & Deferred）

**Must-Confirm**

- OQ-001：边界碰撞实现方式 → 结论：使用 4 个 StaticBody2D 边界板（Top/Bottom/Left/Right），ball.gd 通过 body_entered 信号检测
- OQ-002：物理步长 → 结论：Godot 默认 fixed_process 1/60s，无需修改

**Deferred**

- DEF-001：ball.gd 原 BOUNCE_DAMPING 0.95 → 迁移后 Godot 物理默认弹性系数 0.0，需在材质上设置 restitution=0.95

## 约束引用

约束引用: `.cursor/skills/godot-physics/SKILL.md`
| 相关项 | 匹配依据与关联点 | 本需求处置 |
|---|---|---|
| godot-physics SKILL | move_and_slide() 模式、碰撞层定义 | 直接复用 |
| 20250101130000-Pad发射台系统 | launch() 接口契约 | 保持不变 |
| 20250101120001-物理系统基础（inbox Req 1） | 原 Gate-0 已通过，本需求是其实现细化 | 替代原 Req 1 的实现路径 |

## 数据流闭环表

| 能力 | 来源(Source) | 加工(Process) | 去向(Sink) | 前端入口 | 相关表 | 闭环 |
|---|---|---|---|---|---|---|
| 球体运动 | PhysicsServer2D / move_and_slide | 速度积分 + 碰撞响应 | position 更新 | — | — | 已确认 |
| 碰撞检测 | body_entered 信号 | 对撞体类型判断 | 回调 block.gd / boundary | — | — | 已确认 |
| 速度管理 | launch() / _physics_process | 方向归一化 + 速度衰减 | 物理步进 | — | — | 已确认 |

## 原版对等基线（完全对标，新增 · 关联 R17）

- 球体物理须严格对齐原版 `el/Ball.hx`：无重力纯弹跳，速度模长恒定，反弹角度由入射法向决定（`velocity.bounce(normal)` 等价）
- **当前偏离（须修正）**：`game/scripts/entities/ball.gd` 仍用手写 `step_physics()`（`position += velocity*delta`）+ `check_boundaries()` 手动钳制，违反 ADR-001 与物理对等；由 R17 负责重构为 `move_and_slide()`
- 弹性/速度常量换算：原版帧节拍 → Godot 单位/秒，数值对齐由 R16/R17 提供
- 真机手感验收：见 R17 AC-5

## 原型对齐与偏离

参照源项目 `frontend/src/haxe/el/Ball.hx` 的物理逻辑（速度+方向+弹跳），Godot 侧用 CharacterBody2D 替代手动 update()。偏离点：原版 Pixi.js 无内置物理，Godot 侧需适配 Physics2D 约束。

# Gate-1 方案与验收

## 页面布局预览

不适用（无 UI 触达）

## 验收标准

- [ ] **AC-1**：ball.gd 使用 `move_and_slide()` 进行物理步进，而非手动 position += velocity * delta
  - **反例（本 AC 排除）**：代码中仍存在手动 position 赋值（非 move_and_slide 路径）
- [ ] **AC-2**：CollisionShape2D 为 CircleShape2D，radius = 10，ball.tscn 加载无报错
  - **反例（本 AC 排除）**：形状为空或类型错误
- [ ] **AC-3**：球体从 Pad 发射后，碰到边界（StaticBody2D）正确反弹（velocity 分量取反）
  - **反例（本 AC 排除）**：球体穿过边界或反弹方向错误
- [ ] **AC-4**：launch(direction) 接口签名不变，接收归一化 Vector2 并设置初始速度
  - **反例（本 AC 排除）**：接口签名变更导致 Pad.gd 编译失败
- [ ] **AC-5**：单元测试验证球体在 60fps 下速度衰减正确（restitution 0.95 模拟）
  - **反例（本 AC 排除）**：测试断言失败

## 实现方案

- 架构 / 关键改动：重写 ball.gd 物理部分；在 Game.tscn 的 World 节点下添加 4 个边界 StaticBody2D；Ball.tscn 碰撞材质设置 restitution
- 复用映射 / 代码落点：

| 已有模块/服务 | 可复用接口/函数/表 | 关系类型 | 精确坐标 |
|---|---|---|---|
| ball.gd（现有） | launch(), get_move_velocity(), is_launched_flag(), get_type() | 保留接口签名 | `game/scripts/entities/ball.gd:24-44` |
| godot-physics SKILL | move_and_slide() 模板 | 直接复用 | `.cursor/skills/godot-physics/SKILL.md:20` |
| 20250101120001（inbox Req 1） | AC-1..5 原验收标准 | 继承并重写 | `docs/requirements/inbox/20250101120001-物理系统基础.md` |

- 影响面与回归范围：ball.gd 重写（接口保持兼容）；Game.tscn 需增加边界节点；pad.gd 无需修改（接口兼容）
- 切片拆解：

  1. [AC-1, AC-4] 重构 ball.gd 使用 move_and_slide()
     - Files: `game/scripts/entities/ball.gd`
     - Test: `tests/test_physics/test_ball_physics.gd`
     - Steps: 红测（旧实现）→ 重写 _physics_process 用 move_and_slide() → 绿测
     - Done: 单元测试断言 move_and_slide 调用后位置变化符合预期
  2. [AC-2] Ball.tscn 碰撞形状与材质
     - Files: `game/scenes/entities/Ball.tscn`
     - Test: Godot Editor 检查
     - Steps: 编辑 Ball.tscn，确认 CircleShape2D radius=10，添加 PhysicMaterial restitution=0.95
     - Done: Godot Editor 选中 Ball.tscn 检查 Inspector
  3. [AC-3] 边界反弹
     - Files: `game/scenes/main/Game.tscn`
     - Test: 场景运行测试
     - Steps: 在 World 下添加 4 个 StaticBody2D 边界板，ball 碰撞后 move_and_slide 自动反弹
     - Done: Godot Editor 运行 Game.tscn，球碰到边界反弹

- 回归验证点：
  - `godot --path game --headless --build-sceens game/scenes/main/Game.tscn`
  - `godot --path game --headless --quit --equation "var b=preload('res://scripts/entities/ball.gd').new(); print(b.get_move_velocity())"`

# Gate-2 验收

## 验收记录 — 球体物理系统完整化 2025-01-01

| AC | 结论 | 验证方式 | 证据（类型 + 出处） | 结果摘要 |
|---|---|---|---|---|
| AC-1 | 待验收 | 代码审查 | ball.gd 源码 | — |
| AC-2 | 待验收 | Godot Editor Inspector | Ball.tscn 节点属性 | — |
| AC-3 | 待验收 | Godot Editor 运行场景 | 球碰边界反弹视频/截图 | — |
| AC-4 | 待验收 | 代码审查 | launch() 签名不变 | — |
| AC-5 | 待验收 | Godot Test Runner | 单元测试输出 | — |

- 变更文件：`game/scripts/entities/ball.gd`, `game/scenes/entities/Ball.tscn`, `game/scenes/main/Game.tscn`
- 遗留 / 风险 / Deferred：DEF-001（restitution 值后续根据手感微调）
- 一条可复跑验证命令：`godot --path game --headless --build-sceens game/scenes/main/Game.tscn`

## 真机独立验收（USB 调试模式 · 强制）

> 标准来源：`docs/requirements/execution-plan.md` §真机独立验收标准（USB 调试模式）。
> 原则：**不依赖任何未完成的上游需求，独立构建 debug 包并在 USB 真机上验收**。

| 项 | 内容 |
|---|---|
| 验收入口（debug 场景） | `game/debug/R03_physics_debug.tscn` |
| 入口注册 | `DebugLauncher`（`game/debug/debug_launcher.tscn`）菜单项 `R03 球体物理系统完整化` |
| 依赖解耦 / Mock 基线 | 无（自含 Ball + 4 个 StaticBody2D 边界，不依赖 R01/R02） |
| 验收类型 | 真机交互 + 断言（`move_and_slide` 驱动、边界反弹） |

**验收步骤（可复跑）**

```bash
# 1 构建本需求独立 debug 包
godot --path game --headless --export-debug "Android" game/bin/AlphaBounce_debug.apk
# 2 USB 安装 + 启动
adb -s <DEVICE_SERIAL> install -r game/bin/AlphaBounce_debug.apk
adb shell am start -n com.eternaltwin.alphabounce/com.godot.game.GodotAppLauncher
# 3 在 DebugLauncher 中点选 R03 进入本需求验收入口
# 4 AC 断言日志（须全部 PASS）
adb logcat -d | grep "R03_AC"
# 5 截图取证
adb shell screencap -p /sdcard/R03.png && adb pull /sdcard/R03.png docs/evidence/R03.png
# 6 日志门禁：零 Godot ERROR / SCRIPT ERROR
adb logcat -d -v brief | grep -iE "godot|script error"
```

**AC 自检映射**：debug 场景内对每个 AC 打印 `print("R03_AC-n PASS/FAIL")`，与 AC-1..AC-5 一一对应；Gate-2 证据须含 `docs/evidence/R03.png` + 全部 AC PASS 的 logcat 片段 + 零 ERROR 结论。

# Gate-3 沉淀

## 实现记录与沉淀

- Experience Reuse: `.cursor/skills/godot-physics/SKILL.md` move_and_slide 模式
- Capability Index: no-op
- Living Docs: updated AGENTS.md（ball.gd 状态更新）
- Flow: no-op
- Patterns: `docs/patterns/characterbody2d-ball.md`
- Pitfalls: `docs/pitfalls/restitution-missing.md`（忘记设置 PhysicMaterial 导致球不弹）
