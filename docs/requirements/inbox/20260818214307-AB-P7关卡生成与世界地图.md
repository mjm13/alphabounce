---
标题: AB-P7 关卡生成与世界地图
状态: 待处理
负责人: meijianming
创建时间: 2026-08-18
分级: 黄
类型: 技术
openspec变更:
分级理由: 确定性程序生成 + 27 星球地图；依赖 P1/P2 关卡与砖块，可独立验收
类型判型结论: 技术；Godot 确定性 PRNG + 地图状态机
DDD主类: D
Gate-0: 状态:complete；审批人:meijianming；2026-08-18
Gate-1: 状态:待批准；审批人:<git user.name>；2026-08-18
Gate-2: 状态:待验收；审批人:<git user.name>；2026-08-18
---

# AB-P7 关卡生成与世界地图

# Gate-0 澄清与范围

## 原始诉求（verbatim，禁止改写）

> 复刻计划 P7：确定性程序生成 + 27 星球推进地图。

## 歧义登记

歧义登记: none（触达面：关卡生成与世界地图；确认人:meijianming；日期:2026-08-18）

## 业务目标

不适用（类型=技术）

## 用例 / 用户故事

不适用（类型=技术）

## 范围与切片

> 背景：依赖 P0 确定性 PRNG、P1/P2 关卡与砖块；实现可复现关卡与世界地图推进。
> In Scope 只写能力边界；可执行验收见 Gate-1 `## 验收标准`。

### In Scope（本期交付）

- 确定性关卡生成器（同 seed 复现，参考 `Random.hx`/`Level.hx`）
- 关卡难度参数（基于星球/进度）
- 27 星球节点地图、迷雾/解锁状态
- Earth 终点与地图碎片（Sonar）收集触发结局
- 星球图状态机

### Out of Scope

- 经济/商店/飞船（影响地图推进的元进度）→ P8
- 单关玩法细节 → P1–P6

### 待确认问题与延期项（Open Questions & Deferred）

**Must-Confirm**

- OQ-001：关卡生成参数模型（星球数/砖块密度/敌人分布公式）→ 结论：[待确认，参考设计文档]
- OQ-002：星球解锁条件与碎片收集规则 → 结论：[待确认]

**Deferred**

- 元进度对地图影响（P8）

## 约束引用

| 约束 | 来源 | 说明 |
|---|---|---|
| 复刻计划 | `document/复刻计划.md` §5 P7 | 范围/资产 |
| 原版生成 | `E:\Project\Self\WebGamesArchives\Alphabounce\client\src\Random.hx` | 确定性随机对照 |
| 现代生成 | `E:\Project\Self\EternalTwin-Alphabounce\frontend\src\haxe\Level.hx` | 关卡生成对照 |
| 设计文档 | `E:\Project\Self\EternalTwin-Alphabounce\doc\alphabounce_ds_guide.txt` | 星球/结局说明 |

## 数据流闭环表

| 能力 | 来源(Source) | 加工(Process) | 去向(Sink) | 闭环 |
|---|---|---|---|---|
| 关卡生成 | seed + 参数 | 确定性 PRNG 布局 | 关卡场景 | 已确认 |
| 地图推进 | 通关事件 | 状态机更新 | 存档(P11)/UI(P9) | 已确认 |

## 原型对齐与偏离

无原型对照（资产 `navi/Map.hx`、`worldAsteroMap/`、`mcMapAsteroide/`、`mcMapIcon*/`、`landerPlan*/` 复用）。

# Gate-1 方案与验收

## 验收标准

- [ ] **AC-1**：同一 seed 生成完全一致关卡（可复现/replay）
  - **反例（本 AC 排除）**：同 seed 不同结果
- [ ] **AC-2**：27 星球地图可导航，迷雾/解锁正确
  - **反例（本 AC 排除）**：无法解锁或乱序
- [ ] **AC-3**：到达 Earth 触发结局
  - **反例（本 AC 排除）**：终点无结局

## 实现方案

- 复用映射 / 代码落点：`android/systems/LevelGen.gd`、`android/systems/WorldMap.gd`、`android/resources/LevelParams.gd`
- 切片拆解：

| 切片 | 覆盖 AC | 代码落点 | 验证命令 |
|---|---|---|---|
| 确定性生成 | AC-1 | `LevelGen.gd` + PRNG | 断言同 seed |
| 地图状态机 | AC-2 | `WorldMap.gd` | 手动 |
| 结局触发 | AC-3 | `WorldMap` 终点 | 手动 |

- 回归验证点：
  - 固定 seed 两次生成比对一致；地图推进至 Earth 触发结局

# Gate-2 验收

## 验收记录

| AC | 结论 | 验证方式 | 结果摘要 |
|---|---|---|---|
| AC-1..3 | 通过/未过 | `<seed 比对/Godot 地图测试>` | — |

- 变更文件：...
- 遗留 / 风险 / Deferred：none
- 一条可复跑验证命令：`<Godot 运行 worldmap_test + seed 断言>`

# Gate-3 沉淀

## 实现记录与沉淀

- Experience Reuse: none
- Capability Index: no-op
- Living Docs: updated | no-op
- Flow: updated | no-op
- Patterns: no-op
- Pitfalls: no-op
