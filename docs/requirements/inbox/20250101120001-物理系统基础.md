---
标题: 20250101120001-物理系统基础
状态: 待处理
负责人: Agnes
创建时间: 2025-01-01
分级: 黄
类型: 技术
种子: true
openspec变更:
分级理由: 工程基线；物理系统核心代码，非核心业务规则
类型判型结论: 技术；游戏核心系统
DDD主类: B
Gate-0: 状态:已通过；审批人:Agnes；2025-01-01
Gate-1: 状态:已批准；审批人:meijianming；审批时间:2026-08-28
Gate-2: 状态:未验收
---

# 20250101120001-物理系统基础

# Gate-0 澄清与范围

## 原始诉求（verbatim，禁止改写）

> 实现球体物理系统基础框架。

## 歧义登记

歧义登记: none（已逐句复核，触达面：物理系统；确认人:Agnes；日期:2025-01-01）

## 业务目标

实现 AlphaBounce 核心物理机制：球体弹射、碰撞检测、方块消除。

## 用例 / 用户故事

- 作为玩家，我能发射球体并使其在屏幕上弹跳
- 作为玩家，球体能与方块发生碰撞并消除方块
- 作为开发者，我需要可测试的物理系统

## 范围与切片

> 背景：Godot 4.x 项目；技术栈 GDScript + Godot Physics2D。
> In Scope 只写能力边界；可执行验收见 Gate-1 `## 验收标准`。

### In Scope（本期交付）

- Ball 节点类：位置、速度、类型管理
- 网格坐标系统：世界坐标 ↔ 网格坐标转换
- 碰撞检测：球体与方块、边界碰撞
- 物理更新循环：速度、加速度、摩擦

### Out of Scope

- 不做特殊球体效果（火焰、冰冻等）（原因：后续需求）
- 不做导弹系统（原因：独立需求）
- 不做敌人 AI（原因：独立需求）
- Gate-1 前不接入真实关卡数据（原因：数据驱动设计）

### 待确认问题与延期项（Open Questions & Deferred）

**Must-Confirm**

- OQ-001：物理步长 → 结论：固定 1/60s

**Deferred**

- （无则写无）

## 约束引用

约束引用: `.cursor/skills/godot-physics/SKILL.md`

## 数据流闭环表

| 能力 | 来源(Source) | 加工(Process) | 去向(Sink) | 闭环 |
|---|---|---|---|---|
| 球体物理 | Input | Physics2D | Block 碰撞 | 已确认 |
| 碰撞响应 | BodyEntered | 速度反射 | 位置更新 | 已确认 |

## 原型对齐与偏离

参照源项目 `frontend/src/haxe/Element.hx` 和 `Game.hx` 中的物理逻辑。

# Gate-1 方案与验收

## 验收标准

- [ ] **AC-1**：Ball 类可创建并设置初始位置和速度
  - **反例（本 AC 排除）**：无法初始化
- [ ] **AC-2**：物理更新循环正确移动球体
  - **反例（本 AC 排除）**：球体不移动
- [ ] **AC-3**：边界碰撞正确反弹
  - **反例（本 AC 排除）**：球体穿过边界
- [ ] **AC-4**：网格坐标转换正确
  - **反例（本 AC 排除）**：坐标计算错误
- [ ] **AC-5**：单元测试通过
  - **反例（本 AC 排除）**：测试失败

## 实现方案

- 复用映射 / 代码落点：`game/scripts/entities/ball.gd`
- 切片拆解：

| 切片 | 覆盖 AC | 代码落点 | 验证命令 |
|---|---|---|---|
| Ball 基础类 | AC-1 | `game/scripts/entities/ball.gd` | 单元测试 |
| 物理循环 | AC-2, AC-3 | `game/scripts/entities/ball.gd` | 场景测试 |
| 网格系统 | AC-4 | `game/scripts/core/grid.gd` | 单元测试 |
| 测试 | AC-5 | `tests/test_physics/` | Godot Test Runner |

- 回归验证点：
  - `godot --path game --headless --build-sceens tests/test_physics/test_ball.tscn`

# Gate-2 验收

## 验收记录

| AC | 结论 | 验证方式 | 结果摘要 |
|---|---|---|---|
| AC-1 | 通过 | Godot headless 单测 `_test_ac1_create_and_set` | PASS：初始位置/速度可设置（exit 0） |
| AC-2 | 通过 | Godot headless 单测 `_test_ac2_physics_loop` | PASS：step_physics 积分位移正确（exit 0） |
| AC-3 | 通过 | Godot headless 单测 `_test_ac3_boundary_bounce` + `_test_ac3_block_collision` | PASS：边界反弹+方块碰撞法向反射（exit 0） |
| AC-4 | 通过 | Godot headless 单测 `_test_ac4_grid_conversion` | PASS：world↔grid 双向转换正确（exit 0） |
| AC-5 | 通过 | Godot headless 端到端 `test_ball.tscn` + 真机 android-debug 引擎启动闭环 | **ALL PHYSICS TESTS PASSED**（exit 0，同引擎）；真机：构建→安装→启动→主菜单渲染→logcat 零 Godot ERROR（证明该引擎在真机可运行） |

> **范围说明（2026-08-28 真机实测后补）**：`Game.tscn` / `game.gd` 当前**未实例化/发射球体**（`game.gd` 仅置 HUD，无 Ball/Block 节点与 `launch()` 调用），故真机上进入 Game 场景后画面为静态 HUD、无球运动——**属预期，非本需求范围**。本需求为「物理系统基础」，交付物是物理类（`ball.gd`/`grid.gd`）+ headless 单测；游戏内物理接入为后续需求。因此真机证据定位为「**引擎在真机可构建/安装/启动且无 Godot 错误**」，物理*行为*正确性由 headless `test_ball.tscn`（同一 Godot 4.7.1 引擎）证明，不依赖真机内观察到球运动。

- 变更文件：`game/scripts/entities/ball.gd`、`game/scripts/core/grid.gd`、`tests/test_physics/test_ball.gd`、`tests/test_physics/test_ball.tscn`
- 遗留 / 风险 / Deferred：none
- 可复跑验证命令：
  - 引擎单测：`tools/godot/Godot_v4.7.1-stable_win64_console.exe --path game --headless res://tests/test_physics/test_ball.tscn`
  - 真机闭环：见下方验证证据

### 验证证据（2026-08-28）

**1. 引擎单测（headless）**
```
Godot Engine v4.7.1.stable
PASS: AC-1 初始位置可设置
PASS: AC-1 初始速度可设置
PASS: AC-2 物理循环按速度积分移动球体
PASS: AC-3 边界碰撞法向速度反向
PASS: AC-3 边界钳制位置
PASS: AC-3 方块碰撞法向反射
PASS: AC-4 world->grid 转换
PASS: AC-4 grid->world 转换
ALL PHYSICS TESTS PASSED
EXIT_CODE=0
```

**2. 真机 android-debug 闭环（设备 49PZY5AQR4ZPPBV4 / Xiaomi 25060RK16C）**

| 步骤 | 命令/操作 | 结果 |
|------|-----------|------|
| 构建 | Godot 4.7.1 console `--export-debug Android` | EXIT_CODE=0，APK 已签名 |
| 安装 | `adb -s 49PZY5AQR4ZPPBV4 install -r bin/AlphaBounce_debug.apk` | Success |
| 启动 | `am start -n com.eternaltwin.alphabounce/com.godot.game.GodotAppLauncher` | 前台启动 |
| 截图 | `screencap -p` → pull `device_shot.png` | AlphaBounce 主菜单正常渲染（标题+开始游戏/退出） |
| 日志 | `logcat -d` 过滤 Godot/ERROR/SCRIPT | **零 Godot ERROR/SCRIPT ERROR**；稳定 ~120fps |
| 产物 | `game/device_shot.png` | 截图已保存至 workspace |

> **撤销验收（2026-08-28，原状态）**：AC-1~AC-4 仅「代码就绪，待引擎执行」、AC-5「未执行」，未走真机验收闭环；依 `42-verification-output.mdc` 第 11 条「真机验收门禁」重置 Gate-2=未验收。
>
> **2026-08-28 补充验证**：上述撤销条件已消除——引擎单测全通过（exit 0）+ 真机构建/安装/启动/截图/logcat 闭环完成，无 Godot 错误。等待用户 Gate-2 签字后可标记已验收。

## 原版对等基线（完全对标，新增）

- 本需求为物理基础框架；游戏内物理接入由 R03/R17 完成。
- 原版物理逻辑见 `el/Ball.hx`，须对齐：无重力纯弹跳、反弹角度由法向决定。
- 当前 `ball.gd` 手写速度违反 ADR-001，由 R17 修正；真实关卡数据由 R16 提供。

## 原版对等基线（完全对标，新增）

- 本需求为物理基础框架；游戏内物理接入由 R03/R17 完成。
- 原版物理逻辑见 `el/Ball.hx`，须对齐：无重力纯弹跳、反弹角度由法向决定。
- 当前 `ball.gd` 手写速度违反 ADR-001，由 R17 修正；真实关卡数据由 R16 提供。

## 真机独立验收（USB 调试模式 · 强制）

> 标准来源：`docs/requirements/execution-plan.md` §真机独立验收标准（USB 调试模式）。
> 原则：**不依赖任何未完成的上游需求，独立构建 debug 包并在 USB 真机上验收**。

| 项 | 内容 |
|---|---|
| 验收入口（debug 场景） | `game/debug/BASE1_debug.tscn` |
| 入口注册 | `DebugLauncher`（`game/debug/debug_launcher.tscn`）菜单项 `BASE-1 物理系统基础` |
| 依赖解耦 / Mock 基线 | 无（基础定理已 Gate-2；本入口自含 ball + grid，Game 接入由 R03/R17 负责） |
| 验收类型 | 真机闭环（物理断言 + 引擎可运行） |

**验收步骤（可复跑）**

```bash
# 1 构建本需求独立 debug 包
godot --path game --headless --export-debug "Android" game/bin/AlphaBounce_debug.apk
# 2 USB 安装 + 启动
adb -s <DEVICE_SERIAL> install -r game/bin/AlphaBounce_debug.apk
adb shell am start -n com.eternaltwin.alphabounce/com.godot.game.GodotAppLauncher
# 3 在 DebugLauncher 中点选 BASE-1 进入本需求验收入口
# 4 AC 断言日志（须全部 PASS）
adb logcat -d | grep "BASE1_AC"
# 5 截图取证
adb shell screencap -p /sdcard/BASE1.png && adb pull /sdcard/BASE1.png docs/evidence/BASE1.png
# 6 日志门禁：零 Godot ERROR / SCRIPT ERROR
adb logcat -d -v brief | grep -iE "godot|script error"
```

**AC 自检映射**：debug 场景内对每个 AC 打印 `print("BASE1_AC-n PASS/FAIL")`，与 AC-1..AC-5 一一对应；Gate-2 证据须含 `docs/evidence/BASE1.png` + 全部 AC PASS 的 logcat 片段 + 零 ERROR 结论。

# Gate-3 沉淀

## 实现记录与沉淀

- Experience Reuse: Haxe Element.hx → GDScript ball.gd
- Capability Index: no-op
- Living Docs: updated → AGENTS.md（指针 .cursor→.codebuddy）
- Flow: updated
- Patterns: 物理循环模式
- Pitfalls: Godot Physics2D 与自定义物理的差异
