---
标题: AB-P9 UI/菜单/i18n
状态: 待处理
负责人: meijianming
创建时间: 2026-08-18
分级: 黄
类型: 技术
openspec变更:
分级理由: 完整界面与多语言；依赖 P1–P8 各系统 UI 触点，可独立验收
类型判型结论: 技术；Godot Control/UI + TranslationServer
DDD主类: D
Gate-0: 状态:complete；审批人:meijianming；2026-08-18
Gate-1: 状态:待批准；审批人:<git user.name>；2026-08-18
Gate-2: 状态:待验收；审批人:<git user.name>；2026-08-18
---

# AB-P9 UI/菜单/i18n

# Gate-0 澄清与范围

## 原始诉求（verbatim，禁止改写）

> 复刻计划 P9：完整界面与多语言。

## 歧义登记

歧义登记: none（触达面：UI/菜单/i18n；确认人:meijianming；日期:2026-08-18）

## 业务目标

不适用（类型=技术）

## 用例 / 用户故事

不适用（类型=技术）

## 范围与切片

> 背景：聚合各系统 UI 触点；建立 i18n 框架。纯单机。
> In Scope 只写能力边界；可执行验收见 Gate-1 `## 验收标准`。

### In Scope（本期交付）

- 主菜单、暂停、HUD、设置（Option/Pref）、关卡内 UI、商店/地图 UI
- i18n 框架：en + zh（预留 de/es），复用原版 `Text*.hx` 字符串
- UI 与逻辑解耦（信号/事件）

### Out of Scope

- 单机玩法本体 → P1–P8
- 后端账号/云存档 UI → 本期不做（纯单机）

### 待确认问题与延期项（Open Questions & Deferred）

**Must-Confirm**

- OQ-001：UI 风格与布局（参考 EternalTwin 还是原版 FLA）→ 结论：[待确认]
- OQ-002：i18n 首期语言范围（en+zh 或更多）→ 结论：[待确认，建议 en+zh]

**Deferred**

- 后端账号 UI（Stretch）

## 约束引用

| 约束 | 来源 | 说明 |
|---|---|---|
| 复刻计划 | `document/复刻计划.md` §5 P9 | 范围/资产 |
| 原版文本 | `E:\Project\Self\WebGamesArchives\Alphabounce\web\xml\{de,en,es,fr}` | 多语言字符串 |
| 现代文本 | `E:\Project\Self\EternalTwin-Alphabounce\frontend\src\haxe\Text{De,En,Es,Fr}.hx` | 多语言字符串 |

## 数据流闭环表

| 能力 | 来源(Source) | 加工(Process) | 去向(Sink) | 闭环 |
|---|---|---|---|---|
| 界面导航 | 用户输入 | 场景/面板切换 | 渲染 | 已确认 |
| 语言切换 | 设置 | TranslationServer | 全局文本 | 已确认 |

## 原型对齐与偏离

无原型对照（资产 `navi/menu/*`、`mcMenu*/`、`mcOption/`、`mcPref*/` 复用）。

# Gate-1 方案与验收

## 验收标准

- [ ] **AC-1**：主要界面（主菜单/暂停/HUD/设置/商店/地图）齐全可用
  - **反例（本 AC 排除）**：缺失关键界面
- [ ] **AC-2**：中英文切换正确，无遗漏硬编码文本
  - **反例（本 AC 排除）**：切换后部分文本不变
- [ ] **AC-3**：设置项生效（音量/操作等）
  - **反例（本 AC 排除）**：设置无效果

## 实现方案

- 复用映射 / 代码落点：`android/ui/`、`android/autoload/I18n.gd`、`android/data/i18n/*.csv`
- 切片拆解：

| 切片 | 覆盖 AC | 代码落点 | 验证命令 |
|---|---|---|---|
| 界面场景 | AC-1 | `ui/*` | 手动 |
| i18n 框架 | AC-2 | `I18n.gd` + CSV | 语言切换测试 |
| 设置生效 | AC-3 | `ui/Settings` | 手动 |

- 回归验证点：
  - Godot 运行 UI 流程 + 中英文切换验证

# Gate-2 验收

## 验收记录

| AC | 结论 | 验证方式 | 结果摘要 |
|---|---|---|---|
| AC-1..3 | 通过/未过 | `<Godot UI 测试>` | — |

- 变更文件：...
- 遗留 / 风险 / Deferred：none
- 一条可复跑验证命令：`<Godot 运行 ui_test 场景>`

# Gate-3 沉淀

## 实现记录与沉淀

- Experience Reuse: none
- Capability Index: no-op
- Living Docs: updated | no-op
- Flow: updated | no-op
- Patterns: no-op
- Pitfalls: no-op
