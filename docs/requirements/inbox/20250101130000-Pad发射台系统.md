---
标题: 20250101130000-Pad发射台系统
状态: 待处理
负责人: Agnes
创建时间: 2025-01-01
分级: 黄
类型: 技术+业务
种子: true
openspec变更:
分级理由: 核心玩法入口；Pad是Ball发射的唯一通道，缺失则游戏无法运行；触及游戏核心循环
类型判型结论: 技术+业务；Pad既是物理实体（技术）也是玩家交互入口（业务）
DDD主类: B
Gate-0: 状态:待确认
Gate-1: 状态:待批准
Gate-2: 状态:待验收
---

# 20250101130000-Pad发射台系统

# Gate-0 澄清与范围

## 原始诉求（verbatim，禁止改写）

> 实现球体发射台（Pad），支持触摸拖动瞄准、松手发射，球从Pad位置沿瞄准方向射出。

## 歧义登记

歧义登记: none（已逐句复核，触达面：Pad发射台交互；确认人:Agnes；日期:2025-01-01）

## 业务目标

为玩家提供球体发射的交互入口，是 AlphaBounce 核心玩法的第一环。

## 用例 / 用户故事

1. 作为玩家，我触摸屏幕底部区域看到发射台，拖动方向决定球发射角度
2. 作为玩家，松手后球立即沿瞄准方向以固定速度射出
3. 作为玩家，发射台在球发射后保持可见，可再次发射（如有多球）

## 范围与切片

> 背景：Godot 4.x 项目；已有 `ball.gd` 基础物理框架；已有 `game.gd` 主场景骨架；触摸输入映射已配置 `tap_left`/`tap_right`。
> In Scope 只写能力边界。

### In Scope（本期交付）

- Pad 节点类：位置固定于屏幕底部中央，可视化（占位色块即可）
- 触摸瞄准：`InputEventScreenDrag` 检测拖拽方向和角度
- 发射触发：松手时实例化 Ball 并从 Pad 位置沿方向发射
- 发射方向可视化：拖拽时显示瞄准线（虚线或箭头）
- 与 Game 场景集成：Pad 作为 `Game.tscn` 的子节点

### Out of Scope

- 不做多球数量管理（原因：依赖 R07 玩家存档；重开触发：R07 完成后）
- 不做 Pad 位置移动（原因：固定布局；重开触发：新需求 PRD）
- 不做发射音效（原因：R10 音频系统；重开触发：R10 完成后）
- Gate-1 前不接入真实关卡数据（原因：依赖 R02；重开触发：R02 完成后）

### 待确认问题与延期项（Open Questions & Deferred）

**Must-Confirm**

- OQ-001：瞄准方向精度 → 结论：角度量化到 5° 步进即可（避免过于敏感）
- OQ-002：Pad 球速 → 结论：复用 ball.gd 现有 `SPEED = 400.0`，不在此需求内修改

**Deferred**

- DEF-001：发射台位置可配置（当前硬编码 Y=540，viewport_height-60）→ 后续 ADR 评估是否参数化

## 约束引用

约束引用: `.cursor/skills/godot-android-export/SKILL.md`（触摸输入部分）
| 相关项 | 匹配依据与关联点 | 本需求处置 |
|---|---|---|
| godot-android-export | 触摸输入处理模式（InputEventScreenTouch/ScreenDrag） | 直接复用该模式 |
| godot-game-development | Singleton Autoload 模式 | 不采用，Pad 为 Game 子节点 |

## 数据流闭环表

| 能力 | 来源(Source) | 加工(Process) | 去向(Sink) | 前端入口 | 相关表 | 闭环 |
|---|---|---|---|---|---|---|
| 触摸瞄准 | InputEventScreenDrag | 计算角度和方向向量 | Pad._aim_direction | — | — | 已确认 |
| 球发射 | 松手事件 + 方向 | 实例化 Ball.tscn，调用 launch(direction) | 球进入游戏世界 | — | — | 已确认 |
| 瞄准线渲染 | 拖拽过程 | 绘制虚线预览 | 视觉反馈 | — | — | 已确认 |

## 原型对齐与偏离

参照源项目 `frontend/src/haxe/Pad.hx`。Godot 实现使用 Node2D + CanvasItem.draw 绘制瞄准线，而非 Haxe 的 DisplayObject。

# Gate-1 方案与验收

## 页面布局预览

```
┌──────────────────────────────┐
│                              │
│         World (方块区域)      │
│                              │
│                              │
│                              │
├──────────────────────────────┤
│       [  瞄准线预览  ]        │ ← 拖拽时显示
├──────────────┬───────────────┤
│              │   ◉ Pad      │ ← 屏幕底部中央
└──────────────┴───────────────┘
```

## 验收标准

- [ ] **AC-1**：Pad 节点可实例化，位置固定在屏幕底部中央（x=viewport_width/2, y=viewport_height-60）
  - **反例（本 AC 排除）**：Pad 不在屏幕底部或位置不居中
- [ ] **AC-2**：触摸拖动时，Pad 能计算正确的瞄准方向向量（归一化）
  - **反例（本 AC 排除）**：拖动方向与计算方向不一致或为零向量
- [ ] **AC-3**：松手后，正确实例化 Ball 并从 Pad 位置沿瞄准方向发射（调用 ball.launch(direction)）
  - **反例（本 AC 排除）**：球未出现或方向错误
- [ ] **AC-4**：拖拽过程中显示瞄准线（CanvasItem.draw_line 或 Line2D）
  - **反例（本 AC 排除）**：拖拽时无视觉反馈
- [ ] **AC-5**：Godot Editor 打开 Game.tscn 无报错，Pad 节点正确挂载
  - **反例（本 AC 排除）**：Editor 报脚本加载错误或节点引用断裂

## 实现方案

- 架构 / 关键改动：新建 `Pad.gd`（Node2D），作为 `Game.tscn` 的子节点；修改 `Game.tscn` 加入 Pad 节点
- 复用映射 / 代码落点：

| 已有模块/服务 | 可复用接口/函数/表 | 关系类型 | 精确坐标 |
|---|---|---|---|
| ball.gd | `launch(direction: Vector2)` | 直接调用 | `game/scripts/entities/ball.gd:24` |
| game.gd | `_add_score()` / HUD 引用 | 扩展 | `game/scripts/core/game.gd` |
| godot-android-export SKILL | 触摸事件处理模式 | 直接复用 | `.cursor/skills/godot-android-export/SKILL.md` |

- 影响面与回归范围：Game.tscn 节点树变更；ball.gd 无需修改；main_menu.gd 不受影响
- 切片拆解：

  1. [AC-1] Pad 基础节点创建与位置固定
     - Files: `game/scripts/entities/pad.gd`, `game/scenes/entities/Pad.tscn`
     - Test: `tests/test_pad/test_pad_position.gd`
     - Steps: 红测（节点不存在）→ 实现 Pad.gd（extends Node2D，_ready 中 position = get_viewport_rect().size / 2）→ 绿测
     - Done: `godot --path game --headless --quit --equation "var p = preload('res://scripts/entities/pad.gd').new(); print(p.position)"`
  2. [AC-2, AC-4] 触摸瞄准与方向计算
     - Files: `game/scripts/entities/pad.gd`
     - Test: `tests/test_pad/test_pad_aim.gd`
     - Steps: 红测（无 _input 处理）→ 实现触摸拖拽处理 + 瞄准线绘制 → 绿测
     - Done: Godot Editor 运行 Game.tscn，手动拖动验证方向向量与瞄准线一致
  3. [AC-3] 球发射集成
     - Files: `game/scripts/entities/pad.gd`, `game/scenes/main/Game.tscn`
     - Test: `tests/test_pad/test_pad_launch.gd`
     - Steps: 红测（发射无响应）→ 松手时 instantiate Ball + launch() → 绿测
     - Done: `godot --path game --editor`，在 Game 场景中触摸拖动松手，观察球发射

- 回归验证点：
  - `godot --path game --headless --build-sceens game/scenes/main/Main.tscn`（主菜单仍正常）
  - `godot --path game --headless --build-sceens game/scenes/main/Game.tscn`（游戏场景加载无报错）

# Gate-2 验收

## 验收记录 — Pad发射台系统 2025-01-01

- 变更文件（来自 `git diff`）：`game/scripts/entities/pad.gd`, `game/scenes/entities/Pad.tscn`, `game/scenes/main/Game.tscn`

| AC | 结论 | 验证方式 | 证据（类型 + 出处） | 结果摘要 |
|---|---|---|---|---|
| AC-1 | 待验收 | Godot Editor 查看节点位置 | Editor 截图 | — |
| AC-2 | 待验收 | 手动触摸测试 | 实机 Logcat 打印方向向量 | — |
| AC-3 | 待验收 | 实机运行游戏场景 | 球从 Pad 位置发射，方向正确 | — |
| AC-4 | 待验收 | Godot Editor 预览 | 拖拽时瞄准线显示 | — |
| AC-5 | 待验收 | Godot Editor 打开项目 | Editor Output 无报错 | — |

- 遗留 / 风险 / Deferred：none
- 一条可复跑验证命令：`godot --path game --headless --build-sceens game/scenes/main/Game.tscn`

## 原版对等基线（完全对标，新增）

- Pad 控制须对齐原版 `Pad.hx`：触摸瞄准发射、`checkDrone()`（Drone 触发）、`launchCharge()`（Javelot 蓄力）、导弹发射（GUARDIAN 击杀）。
- 瞄准/发射手感对齐原版，输入动作映射见 R09/R18。

## 原版对等基线（完全对标，新增）

- Pad 控制须对齐原版 `Pad.hx`：触摸瞄准发射、`checkDrone()`（Drone 触发）、`launchCharge()`（Javelot 蓄力）、导弹发射（GUARDIAN 击杀）。
- 瞄准/发射手感对齐原版，输入动作映射见 R09/R18。

# Gate-3 沉淀

## 实现记录与沉淀

- Experience Reuse: `.cursor/skills/godot-android-export/SKILL.md` 触摸模式
- Capability Index: no-op
- Living Docs: updated AGENTS.md（更新游戏模块状态表）
- Flow: no-op
- Patterns: `docs/patterns/pad-launch-pattern.md`
- Pitfalls: `docs/pitfalls/pad-touch-ghost.md`（Android 上 Ghost Touch 需过滤）
