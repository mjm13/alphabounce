---
标题: AB-P11 本地存档与进度
状态: 待处理
负责人: meijianming
创建时间: 2026-08-18
分级: 黄
类型: 技术
openspec变更:
分级理由: 本地持久化全部进度；依赖 P7/P8 数据模型，可独立验收
类型判型结论: 技术；Godot FileAccess/ConfigFile + 版本兼容
DDD主类: D
Gate-0: 状态:complete；审批人:meijianming；2026-08-18
Gate-1: 状态:待批准；审批人:<git user.name>；2026-08-18
Gate-2: 状态:待验收；审批人:<git user.name>；2026-08-18
---

# AB-P11 本地存档与进度

# Gate-0 澄清与范围

## 原始诉求（verbatim，禁止改写）

> 复刻计划 P11：本地持久化全部进度。

## 歧义登记

歧义登记: none（触达面：本地存档与进度；确认人:meijianming；日期:2026-08-18）

## 业务目标

不适用（类型=技术）

## 用例 / 用户故事

不适用（类型=技术）

## 范围与切片

> 背景：聚合 P7（星球进度）、P8（矿物/升级/解锁）、设置（P9）的持久化；纯单机 `user://`。
> In Scope 只写能力边界；可执行验收见 Gate-1 `## 验收标准`。

### In Scope（本期交付）

- 存档模型：星球进度、矿物、升级、解锁、设置
- 自动/手动保存；加载
- 编解码与版本兼容（参考 `Codec.hx` 思路）
- 无云存档（本期纯单机）

### Out of Scope

- 云存档/排行榜 → Stretch（本期不做）
- 各系统玩法 → P1–P10

### 待确认问题与延期项（Open Questions & Deferred）

**Must-Confirm**

- OQ-001：存档格式（JSON vs ConfigFile vs 自定义二进制）→ 结论：[待确认，建议 JSON + 版本号]
- OQ-002：版本升级迁移策略（字段增删兼容）→ 结论：[待确认]

**Deferred**

- 云存档（Stretch）

## 约束引用

| 约束 | 来源 | 说明 |
|---|---|---|
| 复刻计划 | `document/复刻计划.md` §5 P11 | 范围/技术 |
| 编解码 | `docs/reference/haxe/Codec.hx` | 存档思路对照（上游 EternalTwin，已入库） |
| 宪法 | `docs/constitution.md` | 纯单机约束 |

## 数据流闭环表

| 能力 | 来源(Source) | 加工(Process) | 去向(Sink) | 闭环 |
|---|---|---|---|---|
| 进度保存 | 各系统状态 | 序列化+版本号 | `user://save.json` | 已确认 |
| 进度加载 | 存档文件 | 反序列化+迁移 | 各系统恢复 | 已确认 |

## 原型对齐与偏离

无原型对照（参考 `Codec.hx` 编解码逻辑）。

# Gate-1 方案与验收

## 验收标准

- [ ] **AC-1**：退出重进进度保留（星球/矿物/升级/解锁/设置）
  - **反例（本 AC 排除）**：重进后归零
- [ ] **AC-2**：版本升级可迁移（旧档兼容）
  - **反例（本 AC 排除）**：升级后旧档损坏
- [ ] **AC-3**：无损坏/丢失（异常场景健壮）
  - **反例（本 AC 排除）**：崩溃导致存档丢失

## 实现方案

- 复用映射 / 代码落点：`android/autoload/SaveSystem.gd`、`android/resources/SaveModel.gd`
- 切片拆解：

| 切片 | 覆盖 AC | 代码落点 | 验证命令 |
|---|---|---|---|
| 存档模型 | AC-1 | `SaveModel.gd` | 单元 |
| 序列化/加载 | AC-1 | `SaveSystem.gd` | 存读往返测试 |
| 版本迁移 | AC-2 | `SaveSystem` 迁移逻辑 | 旧档导入测试 |
| 异常健壮 | AC-3 | `SaveSystem` 容错 | 损坏档测试 |

- 回归验证点：
  - Godot 运行存档测试：存→关→读 往返 + 旧版本迁移

# Gate-2 验收

## 验收记录

| AC | 结论 | 验证方式 | 结果摘要 |
|---|---|---|---|
| AC-1..3 | 通过/未过 | `<Godot 存档测试>` | — |

- 变更文件：...
- 遗留 / 风险 / Deferred：none
- 一条可复跑验证命令：`<Godot 运行 save_test 场景>`

# Gate-3 沉淀

## 实现记录与沉淀

- Experience Reuse: none
- Capability Index: no-op
- Living Docs: updated | no-op
- Flow: updated | no-op
- Patterns: no-op
- Pitfalls: no-op
