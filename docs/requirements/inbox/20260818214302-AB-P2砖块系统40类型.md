---
标题: AB-P2 砖块系统（40+类型）
状态: 待处理
负责人: meijianming
创建时间: 2026-08-18
分级: 黄
类型: 技术
openspec变更:
分级理由: 数据驱动的砖块类型扩展；非核心业务规则，具备明确验收
类型判型结论: 技术；Godot 资源数据与节点预制
DDD主类: D
Gate-0: 状态:complete；审批人:meijianming；2026-08-18
Gate-1: 状态:待批准；审批人:<git user.name>；2026-08-18
Gate-2: 状态:待验收；审批人:<git user.name>；2026-08-18
---

# AB-P2 砖块系统（40+类型）

# Gate-0 澄清与范围

## 原始诉求（verbatim，禁止改写）

> 复刻计划 P2：支撑多种砖块行为的可配置系统（conglomerate）。

## 歧义登记

歧义登记: none（触达面：砖块系统；确认人:meijianming；日期:2026-08-18）

## 业务目标

不适用（类型=技术）

## 用例 / 用户故事

不适用（类型=技术）

## 范围与切片

> 背景：依赖 P1 的砖块节点与碰撞基础；目标为数据驱动、可扩展的砖块类型体系。
> In Scope 只写能力边界；可执行验收见 Gate-1 `## 验收标准`。

### In Scope（本期交付）

- 砖块数据模型（Resource）：耐久、是否可破、得分、掉落字母、特殊行为标识
- 行为策略：至少实现核心类型（普通、高耐久、不可破、多段破坏、特殊触发）
- 砖块破坏特效与掉落字母拾取物生成
- 数据表驱动增删砖块类型，无需改代码即可新增

### Out of Scope

- 敌人/危险类砖块（射击等）→ P6
- 增益减益效果逻辑 → P4（本需求只负责"掉落字母"这一产物，触发由 P4 接管）
- 关卡程序生成 → P7

### 待确认问题与延期项（Open Questions & Deferred）

**Must-Confirm**

- OQ-001：砖块类型数据存放形式（`.tres` Resource 数组 vs JSON 配置）→ 结论：[待确认，建议 `.tres` + `Resource` 类]
- OQ-002：40+ 类型是否本期全部实现，或先核心子集（≥5 种行为）再补齐 → 结论：[待确认，先 ≥5 种行为]

**Deferred**

- 敌人型砖块（P6）

## 约束引用

| 约束 | 来源 | 说明 |
|---|---|---|
| 复刻计划 | `document/复刻计划.md` §5 P2 | 范围/资产/技术要点 |
| 原版砖块逻辑 | `E:\Project\Self\WebGamesArchives\Alphabounce\client\src\Block.hx` | 行为对照 |
| 现代砖块逻辑 | `E:\Project\Self\EternalTwin-Alphabounce\frontend\src\haxe\el\Block.hx` | 行为对照 |
| 设计文档 | `E:\Project\Self\EternalTwin-Alphabounce\doc\alphabounce_ds_guide.txt` | conglomerate 类型说明 |

## 数据流闭环表

| 能力 | 来源(Source) | 加工(Process) | 去向(Sink) | 闭环 |
|---|---|---|---|---|
| 砖块配置加载 | `BlockDef.tres`/JSON | 实例化节点预制 | 关卡场景 | 已确认 |
| 破坏→掉落 | 球碰撞事件 | 耐久判定+生成字母拾取物 | P4 拾取管线 | 已确认 |

## 原型对齐与偏离

无原型对照（资产 `mcBlock/`、`mcBlockSmc/`、`blockMissile/`、`part*/` 直接复用）。

# Gate-1 方案与验收

## 验收标准

- [ ] **AC-1**：通过数据表新增/删除砖块类型，无需修改节点代码
  - **反例（本 AC 排除）**：每加一种砖块都要改 `.gd` 代码
- [ ] **AC-2**：≥5 种行为砖块（普通/高耐久/不可破/多段/特殊触发）正确表现
  - **反例（本 AC 排除）**：仅单一行为
- [ ] **AC-3**：砖块破坏触发特效与字母拾取物掉落
  - **反例（本 AC 排除）**：破坏无反馈、无掉落
- [ ] **AC-4**：行为与原版 `Block.hx` 对照无重大偏差
  - **反例（本 AC 排除）**：破坏逻辑与原版明显冲突

## 实现方案

- 复用映射 / 代码落点：`android/objects/Block.gd`、`android/resources/BlockDef.gd`、`android/data/blocks/`
- 切片拆解：

| 切片 | 覆盖 AC | 代码落点 | 验证命令 |
|---|---|---|---|
| 砖块数据模型 | AC-1 | `BlockDef.gd`（Resource） | 单元/手动 |
| 行为策略节点 | AC-2 | `Block.gd` + 行为子类 | 手动 |
| 破坏特效+掉落 | AC-3 | `FX`、`Pickup` | 手动 |
| 原版对照校验 | AC-4 | 对照 `Block.hx` | 手动比对 |

- 回归验证点：
  - Godot 运行含多类型砖块的测试关，逐一验证行为

# Gate-2 验收

## 验收记录

| AC | 结论 | 验证方式 | 结果摘要 |
|---|---|---|---|
| AC-1..4 | 通过/未过 | `<Godot 测试关验证>` | — |

- 变更文件：...
- 遗留 / 风险 / Deferred：none
- 一条可复跑验证命令：`<Godot 运行 blocks_test 场景>`

# Gate-3 沉淀

## 实现记录与沉淀

- Experience Reuse: none
- Capability Index: no-op
- Living Docs: updated | no-op
- Flow: updated | no-op
- Patterns: no-op
- Pitfalls: no-op
