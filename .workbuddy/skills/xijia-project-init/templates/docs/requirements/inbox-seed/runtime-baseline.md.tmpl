---
标题: {{req_seq}}-本地运行与CI基线
状态: 待处理
负责人: {{author}}
创建时间: {{date}}
分级: 黄
类型: 技术
种子: true
openspec变更:
分级理由: 跨模块运行约定与 CI；非业务规则
类型判型结论: 技术；运行与交付基线
DDD主类: D
Gate-0: 状态:complete；审批人:{{author}}；{{date}}
Gate-1: 状态:待批准；审批人:<git user.name>；{{date}}
Gate-2: 状态:待验收；审批人:<git user.name>；{{date}}
---

# {{req_seq}}-本地运行与CI基线

# Gate-0 澄清与范围

## 原始诉求（verbatim，禁止改写）

> `/xijia:init` 建立本地运行与 CI 基线。

## 歧义登记

歧义登记: none（已逐句复核，触达面：本地运行与 CI；确认人:{{author}}；日期:{{date}}）

## 业务目标

不适用（类型=技术）

## 用例 / 用户故事

不适用（类型=技术）

## 范围与切片

> 背景：`/xijia:init` 运行基线；后端根 `{{backend_path}}`；前端根 `{{frontend_path}}`；技术栈 {{chosen_stack_summary}}。
> In Scope 只写能力边界；可执行验收见 Gate-1 `## 验收标准`。

### In Scope（本期交付）

- 环境样例文件存在且无真实密钥
- AGENTS Build and test commands 可复制（前后端按实际裁剪）
- CI 占位已替换为真实命令，或声明「仅本地 CI」
- 干净环境下可完成一次「安装 → 测试」

### Out of Scope

- 不做云上发布与生产流水线完备化（原因：本需求只建立本地/CI 基线；重开触发：进入正式发布规划）
- 不做生产密钥托管配置（原因：密钥治理需单独审批；重开触发：安全方案与责任人已确认）
- 不做业务功能（原因：由独立 PRD 需求负责；重开触发：业务需求进入 Gate-0）

### 待确认问题与延期项（Open Questions & Deferred）

**Must-Confirm**

- OQ-001：本地 DB 方式（compose / 外部实例 / 嵌入式）→ 结论：[待确认]

**Deferred**

- （无则写无）

## 约束引用

约束引用: none（已检索，触达面：运行与 CI 基线）

## 数据流闭环表

本需求无业务数据流（运行基线）。声明：工程基线无业务数据流。

| 能力 | 来源(Source) | 加工(Process) | 去向(Sink) | 闭环 |
|---|---|---|---|---|
| 本地验证 | AGENTS 命令 | 安装/测试 | 退出码 0 + 日志 | 已确认 |

## 原型对齐与偏离

无原型对照。

# Gate-1 方案与验收

## 验收标准

- [ ] **AC-1**：环境样例文件存在且无真实密钥
  - **反例（本 AC 排除）**：提交含真实密钥的 .env
- [ ] **AC-2**：`AGENTS.md` **Build and test commands** 安装/启动/测试/构建命令均可复制
  - **反例（本 AC 排除）**：命令仍为待补充
- [ ] **AC-3**：CI 占位已替换为真实命令，或 checklist/AGENTS 声明「仅本地 CI」
  - **反例（本 AC 排除）**：仍保留 init 占位命令
- [ ] **AC-4**：按 AGENTS 命令在干净环境下可完成一次「安装 → 测试」
  - **反例（本 AC 排除）**：命令不可复跑或退出码非 0

## 实现方案

- 复用映射 / 代码落点：`AGENTS.md`、环境样例、`.github/workflows/ci.yml`
- 切片拆解：

| 切片 | 覆盖 AC | 代码落点 | 验证命令 |
|---|---|---|---|
| 环境与命令基线 | AC-1, AC-2 | 环境样例、`AGENTS.md` | `<安装/启动命令>` |
| CI 与干净环境验证 | AC-3, AC-4 | `.github/workflows/ci.yml` | `<测试/构建命令>` |

- 回归验证点：`<AGENTS.md 全量测试命令>`
- 安全边界：禁止提交 `.env` 真实凭据，只提交 example。

# Gate-2 验收

## 验收记录

| AC | 结论 | 验证方式 | 结果摘要 |
|---|---|---|---|
| AC-1..4 | 通过/未过 | `<可执行命令>` | — |

- 变更文件：...
- 遗留 / 风险 / Deferred：none
- 一条可复跑验证命令：`<command>`

# Gate-3 沉淀

## 实现记录与沉淀

- Experience Reuse: none（运行基线）
- Capability Index: no-op
- Living Docs: updated | no-op
- Flow: updated | no-op
- Patterns: no-op
- Pitfalls: no-op
