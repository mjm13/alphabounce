---
description: 后端技术统一护栏（架构、契约、持久化、测试、日志、调试、复用；后续所有后端技术相关追加均写入本文件）
alwaysApply: false
defends: "业务实体被物理删除且无操作人可追溯；把 DB 语义 mock 掉导致测试假通过；日志记入 token/密码明文。均为组织约定（软删默认 = team opinion），非模型能力缺口。"
globs:
  - backend/**
---

# 定位说明

后端技术规则的唯一入口。与前端分工：后端 domain / API 承载业务真值；前端负责展示与交互，不复制业务规则。具体运行时、框架与命令见 `AGENTS.md`。项目特有技术约束由 `docs/constitution.md`、ADR 或 pattern 承载。

# 架构与组织

按**业务能力 / 限界上下文**组织代码，模块划分尽量与 `docs/domain`、菜单/BC 一致。分层深度按当前复杂度裁剪，但**不在接口入口内写业务真值**——业务规则放领域/应用层，不沉到工具类。

# 契约与业务边界（硬约束）

- 对外接口须遵循已确认契约（路径、参数、返回语义、错误码）。
- 模块命名与能力边界一致；跨模块耦合须显式走契约，不靠隐式共享状态。
- 契约冲突时**先升级规格/契约，再改实现**；禁止在实现层「猜测修复」。
- 字段含义、权限边界不明时 stop-and-report，不臆测。

# 持久化与删除（硬约束）

业务实体默认**逻辑删除**，写操作须可追溯操作人；物理删除须 ADR 或需求显式声明并经 Gate 确认。

| 规则 | 说明 |
| --- | --- |
| 审计字段 | 新建业务实体须含：删除标记、创建/更新时间、创建/更新操作人 |
| 操作人注入 | 接口 / 应用层统一注入，调用方不得绕过；无登录上下文填系统标识 |
| 删除语义 | 不得对业务实体静默物理删除；统一走软删并刷新更新时间与操作人 |
| 默认查询 | 列表/详情/业务读取默认排除已删记录 |
| 唯一性 | 业务唯一键在未删记录间唯一；软删后可复用 |
| 需求文档 | 涉及 CRUD 的需求/spec/AC 须写明「逻辑删除」，不得默认物理删 |

例外（须 ADR 或需求显式声明）：append-only 审计日志、纯关联中间表、历史表 retrofit 等。

破坏性数据库操作见 `.cursor/rules/22-db-destructive-safety.mdc`（全局门禁，alwaysApply）。

# 测试（硬约束）

与 `xijia-backend-test` 技能联动：**红-绿-重构**，先写失败验证再实现。

- 触达持久化、事务、权限、唯一性等业务规则：须基于**真实数据库行为**验证。
- 外部系统（HTTP/RPC/消息）可 mock；**数据库语义与业务不变量不可 mock**。
- 测试数据须满足领域不变量与契约约束。
- 禁止先实现后补测试；禁止仅用口头验证代替可执行验证。

# 日志输出（硬约束 + 方向）

日志是可观测与排障的**事实源**：须机器可读、可关联、可检索。**本项目实际字段清单、环境变量与脱敏实现见 `AGENTS.md`「Observability」（SSOT）**；本章只写不随实现变化的硬约束。

## 硬约束（Must / Must Not）

| 规则 | 说明 |
| --- | --- |
| 结构化 | 生产环境须**结构化输出**（JSON 或稳定 key=value）；字段名稳定、跨部署不随意改名 |
| 关联 ID | 每个 inbound **请求 / 异步任务 / 消息**在边界注入关联 ID；合法入站 ID 须复用，否则生成并贯穿本次处理 |
| 向下传播 | 调用下游 HTTP/RPC/消息时须传递同一关联上下文（请求头或等价载体） |
| 敏感数据 | **禁止**记录密码、token、API 密钥、完整凭证；PII 最小化（优先内部 ID，非邮箱/手机号明文） |
| 默认不泄 body | 默认**不**记录完整 request/response body；确需排障时须 ADR 或需求显式声明 + 脱敏 |
| 级别语义 | ERROR＝需人工介入；WARN＝可降级/重试/契约边界；INFO＝关键业务事件；DEBUG＝本地/测试细节（生产默认关闭或采样） |
| 写入位置 | 业务日志写在**应用层 / 领域关键节点**；工具类与循环体内避免 info 级日志 |
| 事件命名 | 重要节点使用稳定 **event** 名（如 `order.created`），使其可被检索聚合 |
| 禁止替代 | 不得用临时标准输出代替生产日志；不得为单次排障提交不可关闭的调试输出 |

## 采样与成本

- 高频成功路径可采样；**ERROR 与 rare WARN 不得盲目采样丢弃**。
- 脱敏/redaction 在**统一日志出口**集中处理，不在业务代码逐处手写。

## 与需求 / 技术需求的关系

- `business`/`hybrid` 需求：AC 中涉及异步、支付、权限等关键路径时，须含**可验证的日志/event 断言**或验收说明（如「失败时 ERROR 含 trace_id + event」）。
- `technical` 需求：「可观测性」章节须引用本章硬约束或 `AGENTS.md`「Observability」，写明必填字段与禁止项。

# 工具与复用

**两处消费者规则**：出现第二处真实调用后再下沉到共享层，不为单次需求预先抽象。带业务语义的逻辑进领域/应用层，不进工具类。

# 升级触发（When to Escalate）

- 弱规则演进为强不变量或跨聚合一致性 → 升级分层设计并补 ADR。
- 测试无法覆盖关键业务规则 → 先补验证方案再继续实现。
- 文档与测试冲突 → 以可执行测试为准并回写文档。
- 接口语义不一致、权限边界冲突 → 升级到规格层决策。
- 无法用现有模块/共享层覆盖 → 先定边界与契约，再编码；不 shortcut 跨 BC 耦合。

# 验收口径（Done Means）

- 关键业务规则在真实数据语义下可复现、可验证。
- 交付内容满足契约，可被回归验证（见 `xijia-backend-test`）。
- 删除/审计语义符合「持久化与删除」；默认查询不泄露已删数据。
- 无未经两处消费者验证的过早抽象；无工具类承载业务真值。
- 关键路径日志符合「日志输出」：结构化、有关联 ID、无敏感数据泄露、event 可检索。
- 触发后台请求的调试手段可追溯到测试或结构化日志，而非临时标准输出。
