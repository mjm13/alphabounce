# 2026-08-18

## 任务：将 Cursor 的技能与规则迁移到 WorkBuddy

- 源：`.cursor/skills/`（50 个技能目录）、`.cursor/rules/`（20 个 .mdc 规则）。
- 目标：`.workbuddy/skills/`（项目级技能，自动发现）。
- 结果：
  - 48 个 Cursor 技能原样复制并规范化 SKILL.md frontmatter（`name`/`description` 保留，补 `agent_created: true`）。
  - 2 个非技能共享目录（`references`、`evals`，无 SKILL.md）作为资源目录一并复制，供其它技能相对引用解析。
  - 20 个 `.mdc` 规则转换为 `rule-<name>/SKILL.md` 技能；2 个 alwaysApply 规则（`00-workflow`、`22-db-destructive-safety`）在 description 标注 `[alwaysApply]`。
  - 校验：68 个 SKILL.md 全部 frontmatter 合法，正文无截断。
- 范围外（未做，按需）：`.cursor/hooks/`（hooks.json + python 脚本，WorkBuddy 无等价 hooks 引擎）、`.cursor/commands/`（20 个斜杠命令，可转技能）、`.cursor/mcp.json`（3 个 MCP server，全局 `~/.workbuddy/mcp.json` 未动，避免污染其它项目）。
- 注意：保留 `.cursor/` 原目录不动，技能内 `.cursor/...` 相对引用仍可解析。WorkBuddy 无 alwaysApply 机制，alwaysApply 规则需手动触发或注入 MEMORY.md 才每会话生效。

## 任务：/xijia-init 项目初始化（Alphabounce 安卓复刻）

- 触发：用户按 `Alphabounce源码参考.html` 初始化，目标=复刻该 2D 物理打砖块游戏到安卓。
- 访谈结论：技术栈 **Godot 4（GDScript/C#）+ 安卓导出**；纯单机无后端/DB；首里程碑=可玩 MVP demo；技能策略=仅生成推荐清单（不安装）。
- 产出（空仓库 init，已通过 self-check）：
  - 基座文档：`README.md`、`AGENTS.md`、`docs/`（constitution/README/llms.txt/decisions/0001-project-bootstrap/domain/requirements/openspec/archive/process×4）。
  - 代码壳：`android/`（.gitkeep + README），Godot 工程根。
  - 种子需求：`docs/requirements/inbox/20260818205718-AB-安卓工程初始化.md`（frontend-bootstrap 模板改名，黄档/技术/种子）。
  - 技能推荐：`docs/SKILLS_RECOMMENDATION.md`（0 安装；Top 推荐 systematic-debugging/TDD/verification-before-completion；Godot/安卓专用技能 NotFound）。
  - CI 占位：`.github/workflows/ci.yml`。
- 自检：`policy_flow_drift_check.py` 与 `drift_guard_scan.py` 均 exit 0；requiredFiles/agentsFormat 全绿。
- Next：`/xijia:start docs/requirements/inbox/20260818205718-AB-安卓工程初始化.md`（工程基线 Gate-1 后人审）。

## 任务：下载 Alphabounce 源码（复刻参考）

- 已下载到 `E:\Project\Self\`：
  - `WebGamesArchives/`：sparse-clone `motion-twin/WebGamesArchives`，仅取 `Alphabounce/`（48M）。原版 2007 Haxe/Flash：72 个 .hx（client/src: el/ev/fx/lander/navi）、FLA 源、编译 SWF（gfx/game/inv/planet/adtk）、i18n（de/en/es/fr XML）、web 资源。最保真但 Flash 时代、难直接运行。
  - `EternalTwin-Alphabounce/`：clone `gitlab.com/eternaltwin/alphabounce/alphabounce`（63M，94 个 .hx：common/backend/frontend）。最完整可玩：4179 张 PNG 精灵帧（按元素分目录，可直接导入 Godot）、5 个 woff 字体、Node+Redis 后端、`doc/alphabounce_ds_guide.txt` 设计/玩法文档、TODOLIST（缺 end-mission、若干 bug）。
- 结论：**EternalTwin 作主参考**（架构清晰、精灵齐全、有设计文档），**WebGamesArchives 原版作对照**（机制/平衡/i18n 保真）。
- 更完整源码排查：DSi 零售版闭源不可得；`eternaltwin/alphabounce/libs-haxe` 是其 Haxe 依赖（可选补）；3plus4i GitLab fork 落后上游。无更完整的公开源码。

## 任务：/xijia-start 拆分复刻计划为 13 个需求文档

- 触发：用户基于 `document/复刻计划.md`（P0–P12 共 13 阶段）拆分需求，并在文档末尾用 001–013 编号表示执行顺序。
- 方法：xijia-prd-to-requirement（因 xijia-start 未迁移，用该技能方法论替代）。每篇沿用既有种子格式（frontmatter 标题/状态/分级=黄/类型=技术/Gate-0-2 + Gate-0 范围 + Gate-1 验收/方案 + Gate-2/3 空模板），约束引用指向真实存在的 repo 路径（constitution/decisions/复刻计划/设计文档/源码 .hx）。
- 结果：
  - **001 (P0)** 复用既有种子 `20260818205718-AB-安卓工程初始化.md`，仅将其标题对齐为「AB-P0 工程基座与安卓导出」（内容即工程基线，对应 P0）。
  - **002–013 (P1–P12)** 新建 12 篇需求于 `docs/requirements/inbox/`：`20260818214301-AB-P1核心玩法MVP.md` … `20260818214312-AB-P12安卓打包与发布打磨.md`。
  - 每篇均含 OQ-001/002 待用户确认项（控制方式、数值、补充规则、许可证立场等）。
  - 计划文档追加 `## 9. 需求拆分顺序（001–013）`：编号表（Phase/标题/优先级/依赖/路径）+ 执行建议（三批：地基→深度→体验收尾）。
- 校验：13 篇全部 UTF-8 无乱码、frontmatter 合法（标题/状态/分级/类型/Gate-0-2 齐全）。
- Next：按 001→002→003/004→005… 逐个 `/xijia:start`，Gate-0 人审时拍板各 OQ。

## 任务：/xijia-start 推进 P0（AB-P0 工程基座与安卓导出）→ Gate-2 实施中

- 用户批准 Gate-1（完整安卓构建打通）。进入 Gate-2 实施。
- **Spike-0 结论：continue**。环境代理坑（关键经验，复用）：
  - `commandlinetools-win-<build>_latest.zip` 被代理 **404**（Windows 引导包）；`commandlinetools-linux-11076708_latest.zip` 与 `commandlinetools-mac-*` 返回 200 → 用 **linux 变体替代**，其 `sdkmanager` 是 POSIX 脚本，在 Git Bash + JDK 下需把 POSIX 路径经 `cygpath -w` 转 Windows 路径再 `java -classpath .../sdkmanager-classpath.jar com.android.sdklib.tool.sdkmanager.SdkManagerCli` 调用（已封装 `tools/android-sdk/sdkmanager.sh`）。
  - `dl.google.com` 明显比 GitHub CDN 慢（~110KB/s vs ~700KB/s）；`platform-tools-latest-windows.zip` 的 **GET 返回 0 字节**（仅 HEAD 可达）→ platform-tools/adb 无法自动下载（仅影响真机安装，本环境无设备）。
  - GitHub API 限流；用 `curl -sIL -L .../releases/latest` 取重定向 URL 拿版本号（最新 stable=4.7.1）。
  - 大文件下载需用 `-m 900/1200/1800` 长超时或 `curl -C -` 断点续传，否则被 300/600s 超时截断（Godot tpz 1.28GB 需续传）。
- **已完成并验证**：
  - Godot 4.7.1（`tools/godot/Godot_v4.7.1-stable_win64.exe`，exit 0 跑通）；`android/` 工程（`project.godot` config_version=5 + `scenes/Main.tscn` Node2D/ColorRect/Label + `scripts/main.gd` + `icon.svg`）；`godot --headless --path android --quit` exit 0 且打印 "Alphabounce started" → **AC-1/AC-4 通过**。
  - Temurin JDK 17.0.20（`tools/jdk/jdk17`，java+keytool 可用）；Android SDK `build-tools;34.0.0`(apksigner/zipalign/aapt2)、`platforms;android-34`(android.jar)、`cmdline-tools/latest`(sdkmanager) 装好；`android/debug.keystore`(alias alphabounce) 生成 → **AC-2 部分通过**（缺 platform-tools）。
  - `android/export_presets.cfg`：真实键名从 Godot 二进制提取（`android/android_sdk_path`、`android/java_sdk_path`、`android/debug_keystore*`、`gradle_build/{min_sdk,target_sdk,export_format,use_gradle_build}`、`package/name`、`version/code/name`、`custom_template/debug|release`）；**未写 keystore 明文密码**（Godot 自动 debug 签名，安全）；`package/name=org.godotengine.alphabounce`。
  - `.gitignore`（排除 tools/、android/build/、debug.keystore、keystore.local.env）；`AGENTS.md` Build/test 命令、测试栈、Secrets 回填（**AC-5 通过**）。
  - 双 guard：`check-intake`/`check-plan` 全绿；`verify_utf8` 通过。
- **阻塞中（后台续传）**：Godot 安卓导出模板 `export_templates.tpz`（1.28GB，已 66%）经代理慢速下载；完成后用 `custom_template/debug` 指向预构建 `android_debug.apk` 跳过 gradle，直接 apksigner/zipalign 导出 `android/build/Alphabounce-debug.apk` 并验签 → 补 **AC-3**。
- 真实风险：本环境无 Android 设备或模拟器，`adb install` 真机验证无法进行（仅能验证 APK 本身有效/可签名）。
- 技术栈决策确认：GDScript（非 C#）；完整安卓构建打通（非仅目录）。
