# 2026-08-19 工作日志

## P2（AB-P2 砖块系统40类型）推进 /xijia:start
- 触发：用户 `/xijia-stater @...20260818214302-AB-P2砖块系统40类型.md`（即 `/xijia:start`）。
- 路由：`/xijia:start` → `xijia-feature-pipeline`（仅委派）→ `xijia-ops-pipeline`。Skill 工具注册表无 `xijia-feature-pipeline`，直接 Read `.workbuddy/skills/xijia-ops-pipeline/SKILL.md`。
- 单回合契约：先 `--resolve-gate --req <file> --format cta` 定位门禁，照贴 CTA，只推进当前门禁。
- P2 初态：Gate-0=complete(非法值)/Gate-1=待批准。--resolve-gate 报「Gate-1 方案缺口：缺可执行的回归验证点（见 AGENTS.md）」。
- 补全（writing-plans 缺口模式，增量 merge 进 Gate-1）：
  - frontmatter `Gate-0: complete` → `已通过；审批人:meijianming；2026-08-18`（对齐合法枚举与 P0 格式）。
  - `回归验证点` 由散文改为可执行命令（引用 AGENTS.md `Build and test commands`：`godot --headless --path android --quit` + `scenes/blocks_test.tscn` 跑出 `ALL BRICK TESTS PASSED`）。
  - 补 `## 页面布局预览`：不适用（无 UI 触达；类型=技术）。
- 结果：`--check-plan` OK(exit 0)；`--resolve-gate` 现输出 **Gate-1 待批准** CTA，待用户回复 `批准 Gate-1`。

## 发现的技能路径漂移（bug，跨会话可复用）
- xijia 技能正文引用 `.cursor/hooks/pipeline_guard.py` / `.cursor/hooks/verify_utf8.py`，但真实路径为：
  - `.workbuddy/hooks/pipeline_guard.py`
  - `.workbuddy/skills/xijia-safe-file-write/scripts/verify_utf8.py`
- 跑 guard 须用真实路径，否则 `can't open file` / exit 2。

## P0（AB-P0）仍未 shipped
- 状态 Gate-2 待验收；AC-1/2/4/5 通过，AC-3（实际导出 APK）仍阻塞于 Godot 安卓导出模板下载。
- tpz 现状 ~873MB / 1280MB（IVXw8s resume-loop 续传中，代理限速极慢）。extracted/ 与 android/build/*.apk 均尚未生成。
- 工作流硬约束「未完成不得开下一需求」与本次先开 P2 冲突——已向用户标注，待其裁定（关 P0 或继续 P2）。

## P2 实施完成 + 验证通过（Gate-1→Gate-2）
- 用户「Gate-1 批准」后，按 CTA 切片 TDD：实现砖块系统并 headless 验证。
- 落点（与文档原计划的分散文件不同，因 Godot headless 不生成全局 class_name 缓存）：
  - `android/scripts/brick_system.gd`：单文件内联类（BlockDef 数据模型 + Behavior 枚举 + Block/FX/Pickup 节点 + BlocksRegistry），「单脚本内联类 + preload」模式，headless 稳定解析。
  - `android/data/blocks/blocks.json`：44 条砖块类型（行为 0:12/1:12/2:6/3:6/4:8，5 类齐全，≥40）。
  - `android/scenes/blocks_test.tscn` + `android/scripts/blocks_test.gd`：遍历全部 44 型断言 5 行为差异 / 破坏FX+字母掉落 / 不可破不销毁 / 原版对照。
- 验证：`godot --headless --path android scenes/blocks_test.tscn` → `Loaded brick types: 44` / `ALL BRICK TESTS PASSED`（exit 0）。AC-1..4 全过；AC-4 对照原版 `Block.hx`（life==null≡不可破、bonusTable→newOption≡掉字母、onDamage≡特殊触发）。
- 误删 P1 的 `android/objects/Block.gd`（`LevelBase.gd` 经 preload 引用，接口 durability/max_durability/game/take_hit），已 `git checkout HEAD --` 恢复，P1 完好。教训：删文件前先 grep 引用。
- 多段砖 bug 已修：伤害溢出逐段传递（一次 999 命中应全清销毁）；GDScript lambda 改标量在闭包中不生效，统一改用数组模式。

## 第二个 xijia guard bug（栈漂移标记错配，已修）
- `guardlib/livingdocs.py` 的 `STACK_TRUTH_MARKERS`/`README_STACK_MARKERS`/`STACK_STALE_PATTERNS` 硬编码为 Web 栈（FastAPI/Vue3/Spring Boot/Maven…），对 Godot/Android 游戏项目永不通过 → `--check-release` 栈漂移阻塞。
- 修复：改为本工程真实栈标记 `("Godot","GDScript","Android")`；新建真相源 `docs/architecture.md`（含 `## compiled_truth`）；修正 README 中 `.cursor/hooks`→`.workbuddy/hooks` 路径（同路径漂移 bug）。
- 现 `--check-release` 客观项通过，P2 进入 Gate-2 待人工签字。

## P0（AC-3）仍阻塞
- tpz 仍 ~873MB/1280MB，后台续传已停；AC-3 未可达。P0 仍 Gate-2 待验收（未 shipped）。
- P2 走完 Gate-2 签字后将进 Gate-3 归档；P0 仍吊着。

## 门禁路径修正（累计）
- guard 真实路径：`.workbuddy/hooks/pipeline_guard.py`（非技能正文写的 `.cursor/hooks/`）。
- `--check-release` 要求顶-level `状态` ∈ {待处理|已交付|已归档|积压}；Gate 状态英文需映射（complete→已通过/approved→已批准/accepted→已验收）。
- `状态: 进行中` 不合规 → 改 `待处理`（inbox 进行态）。

## P0 AC-3 真正执行（用户「下载好环境进行构建，构建完告诉我地址」）
- 重启 tpz 续传（后台 38Gr2a resume-loop，`-C -` 续传 + 每轮 600s 超时自动续；代理限速 ~0.9MB/s，每轮约 53MB）。21:01 续传前 873MB，至 21:11 已 926MB，剩 ~353MB。
- **export_presets.cfg 路径 bug 已修**：原写死 `D:/Project/Self/alphabounce/...`（工程实际在 `E:/`），全改 `E:/`；并把 `package/signed=true`→`false`（Godot headless 拿不到 keystore 密码），改由 `apksigner` 手动签名。
- 写 `tools/build_android.sh`：校验 tpz → 解压到 `tools/godot/templates/4.7.1.stable/` → `godot --headless --export-debug "Android"` 产出未签名 APK → `zipalign` → `apksigner sign`（密钥/别名/密码从 gitignored 的 `android/keystore.local.env` 读取：KEY_ALIAS=alphabounce, KEYSTORE_PASS=alphabounce-dev-2026）→ 验签 → 打印最终路径。
- 预期产物：`android/build/alphabounce-debug.apk`。
- 待 tpz 完成（38Gr2a 通知）后运行该脚本，回填 P0 Gate-2 AC-3 验收记录并关单。

## P0 AC-3 完成 + 验收记录回填（收尾）
- **构建重跑验证通过**：`bash tools/build_android.sh` 端到端跑通（tpz 校验 OK → 解压模板 → headless 导出 → zipalign → apksigner sign v1+v2 → `VERIFY OK`）。产物 `E:\Project\Self\alphabounce\android\build\alphabounce-debug.apk`（28,299,746 字节）。
- **APK 元数据（aapt2 dump badging）**：package `com.example.alphabounce`、versionCode 3 / versionName 1.2、compileSdk/targetSdk 36、minSdk 24。
- **回填 P0 文档 Gate-2**：AC-3 行改为「通过（构建可复现）」，修正产物名 `alphabounce-debug.apk`、加入 `tools/build_android.sh` 一键构建、清理过时的 D:/E: 盘叙事；变更文件清单与遗留段同步更新；可复跑命令改为 `bash tools/build_android.sh`。
- **AGENTS.md 补正**：Build and test commands 新增 `bash tools/build_android.sh` 一键可复现构建，导出/安装文件名统一为 `alphabounce-debug.apk`（原大写 `Alphabounce-debug.apk` 与真实产物不符）。
- 用户原始诉求「下载好环境进行构建，构建完告诉我地址」已完成 → 已向用户报告最终 APK 路径。
- 注意：Gate-2 状态位仍为「待验收」（frontmatter，需用户签字），本次仅回填验收记录证据，未擅自翻转状态。
