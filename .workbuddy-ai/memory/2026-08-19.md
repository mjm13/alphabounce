# 2026-08-19

## AB-P1 核心玩法 MVP 实现（/xijia-start）
- 在 Godot 4.7.1 工程内实现最小可玩闭环：挡板/球/砖块/墙/HUD/胜负/重开。
- 代码落点：`android/objects/Pad.gd`、`Ball.gd`、`Block.gd`、`android/levels/LevelBase.gd`（管理器+状态机+HUD）；`Main.tscn` 根脚本改为 LevelBase（旧 `scripts/main.gd` 停用）。
- 决策：OQ-001 控制=触控点跟随；OQ-002 初始命数=3、单关 9×5=45 硬编码砖块网格。
- `project.godot`：`stretch/aspect=keep`（宽屏手机以左右 letterbox 防裁剪关键 UI）、`handheld/orientation=landscape` 锁横屏。
- 物理：球用 `CharacterBody2D` + `_physics_process` 定步长 `move_and_collide`；挡板命中位置→入射角（最大 60°）；`_normalize` 保证最小垂直分量防水平卡死。
- 已重新导出 `android/build/Alphabounce-debug.apk`（mtime 13:46，含 `--fullscreen`，apksigner v2/v3 通过）。
- 校验：`godot --headless --path android` 跑 8s 无 SCRIPT ERROR（场景构建+物理+碰撞/计分路径跑通）；真机触控手感与铺满效果待用户目测。
- 需求文档 `docs/requirements/inbox/20260818214301-AB-P1核心玩法MVP.md`：Gate-1 置已批准，Gate-2 填实现记录与遗留风险。
- 坑：`class_name` 跨脚本类型在 headless/导出解析失败 → 改用 `preload` 常量 + `obj.get()/call()` + `as Node2D`（已记入 MEMORY.md）。

## AB-P1 横屏左右黑边修复（用户反馈"左右还是有空白"）
- 根因：`stretch/aspect=keep` 在宽于 16:9 的手机横屏以左右 letterbox 保比例 → 黑边。
- 修复：`project.godot` 改 `window/stretch/mode="canvas_items"` → `"viewport"`（根视口=设备分辨率，`aspect` 被忽略）。LevelBase.gd 布局全程用 `get_viewport_rect().size`，无需改游戏代码即自适应满屏。
- ⚠️ 排除 `canvas_items`+`aspect=expand`：超宽屏会裁掉底部挡板（paddle 在 VH-70 被裁出屏外），不可玩。
- 重新导出 `android/build/Alphabounce-debug.apk`（mtime 14:33，含 `--fullscreen`+viewport，apksigner v2/v3 通过；`project.binary` 内 `stretch/mode=viewport` 已确认）。包名仍 `com.example.alphabounce`。
- 同步更新 P1 需求文档 Gate-2 遗留风险段 + MEMORY.md 屏幕适配段（viewport 为满屏正解，expand/keep 均有坑）。
- 待用户真机目测：是否铺满无黑边、触控手感、超宽屏元素比例。

## AB-P1 v1.1 viewport 诊断构建（用户仍反馈"还是一样"）
- 用户装上 14:33 viewport APK 后左右黑边依旧。新加右上版本号 `v1.1 viewport` + 顶部绿色调试行 `VP / win / disp / stretch` 确诊。
- 真机调试读数（决定性证据）：**`VP 1920x1080` / `win 2772x1280` / `disp 2772x1280` / `stretch=viewport`**。根视口卡在设计尺寸，窗口=物理全屏，stretch=viewport 在 Godot 4 安卓预构建模板上**不**把根视口 framebuffer resize 到屏幕——这是安卓预构建模板的渲染管线特性，framebuffer 始终按 `viewport_width/height` 分配，GPU 缩放器按 aspect 投到 surface。
- 否定 `viewport` 模式在安卓上能满屏。改方案：`canvas_items` + `aspect="expand"`（填满裁溢出）+ 安全带布局（挡板 vps.y×0.82、砖块 start_y=200、HUD 落到 y≥110、MessageLabel 缩小到 0.6×vps.y 高、球丢失阈值收紧到 vps.y-10）。
- v1.2/code=3 重新导出（mtime 15:05），apksigner v2/v3 通过，`project.binary` 确认 `stretch/mode=canvas_items` + `aspect=expand`，headless 0 SCRIPT ERROR。版本号 bump 让用户一眼看出是否真装上 v1.2 expand。
- 更新 MEMORY.md 屏幕适配段为正解（`canvas_items`+`expand`+安全带；`viewport` 在安卓预构建无效）。

## AB-P1 满屏已通过用户验收（v1.2 expand）
- 用户装机截图反馈：横屏铺满无黑边、SCORE/LIVES/砖块/挡板/球全部正常。
- **关键发现（要更正 MEMORY.md）**：调试行实测 `VP 2338x1080 / win 2772x1280 / disp 2772x1280 / stretch=canvas_items / aspect=expand`。`canvas_items+expand` 模式下根视口**会被 resize** 到 2338×1080（VP 宽高比与屏一致 = 2772/1280），不再是之前以为的「锁设计尺寸靠 GPU 缩放器」。所以 expand 才是真正让根视口跟屏走的模式；viewport 在安卓预构建上反而不 resize。
- 同步更新 P1 Gate-2 文档：状态→已完成、AC-1/2/4 去掉「真机待验收」、变更文件+遗留风险段改为 v1.2 canvas_items+expand + 安全带描述、配置 `config/version=1.2` 对齐 APK versionName。
- P1 闭环。等待用户决定下一步推进：关闭 P1 / 启动 P2（多球/特殊砖块）。

## 沉淀 Godot 4 安卓满屏技能（可复用工作流）
- 将本次踩坑工作流固化为用户级技能 `godot-android-fullscreen`（路径 `~/.workbuddy-ai/skills/godot-android-fullscreen/`），已通过 skill-creator 校验并打包。
- 内容：viewport 模式在安卓预构建模板不 resize 根视口（黑边根因）、canvas_items+expand 真正满屏的判定签名（VP 宽高比==屏宽高比）、安全带布局公式、屏上 VP/win/disp/stretch/aspect 诊断方法、重导出与验签命令、BUILD_TAG 版本标签。references/diagnostics.md 含可贴 GDScript 与配置片段。

## AB-P1 正式关闭（用户选择「收尾关闭 P1」）
- 更新 P1 需求文档：`关闭: 状态:已关闭；审批人:meijianming；2026-08-19`；Gate-3 Pitfalls 指向 `godot-android-fullscreen` 技能；新增「关闭记录」段（关闭依据/交付物/已知非阻塞遗留/后续路线）。
- 用户选择不重建：游戏内 `BUILD_TAG` 仍 `"viewport"` 与 v1.2 expand 实际构建不符，作为已知非阻塞遗留记录，待日后重导出时改回 `"expand"`。
- P1 阶段彻底结束。后续路线：多球/特殊砖块→P2/P3、关卡程序生成→P7、存档→P11。
