# 2026-08-17

## 任务：获取 Motion Twin《Alphabounce》源码作为参考

- 官方源码仓库：`motion-twin/WebGamesArchives`（Motion Twin 官方 GitHub 组织），《Alphabounce》位于 `Alphabounce/` 子目录。
- 语言：源码用 **Haxe** 编写（MT 自研语言），编译为 Flash SWF；非纯 ActionScript。入口类 `client/src/Manager.hx`，构建用 `client.hxml`/`prod.hxml`/`Makefile`/`swfmake.xml`。
- 结构：`client/`(游戏客户端 Haxe 源码+fla+swf)、`com/`(共享数据/协议类 .hx)、`design/fla/`、`web/`(web 包装/i18n/xml/img)。注意：仅含客户端+数据，不含服务端代码；构建依赖外部 `mt` 库与 `swfmake`，单独编译不易。
- 镜像仓库（deepnight / MT 创始人 Sébastien Bénard）：`deepnight/motionTwinWebGamesArchives`。

## 已落地文件

- 当前工程 `D:\Project\Self\alphabounce\`（远程 `mjm13/alphabounce`，main 分支无提交）→ 已放入 `client/ com/ design/ web/`（仅 Alphabounce 子目录内容，便于研究/改造）。
- 参考资料完整仓库 → `D:\Project\Self\WebGamesArchives\`（完整 `motion-twin/WebGamesArchives`，约 3.3GB，含 DinoRPG/Mush/Hordes/Alphabounce 等 27 个游戏 + libs-haxe2/libs-haxe3 + TwinoidGoals），作为整体参考资料，与 alphabounce 工程分离。

## 备注

- 社区复活项目 Eternal-Twin：`https://alphabounce.eternaltwin.org/`（Flash 停更后的可玩现代替代）。

## 补充：Eternal-Twin 官方重写源码（已确认有，且已克隆）

- 仓库（GitLab，非 GitHub）：`https://gitlab.com/eternaltwin/alphabounce/alphabounce`
- 这是**从零重写的开源版本**（不是原版 SWF 镜像），许可证 AGPL-3.0-or-later。
- 技术栈：Haxe 4.3.7（游戏核心）+ TypeScript/Node 24（backend/frontend）+ Redis（数据库）+ Docker/Podman（OCI 容器）；渲染用 PixiJS（haxelib pixijs）。
- 结构：`common/`(前后端共享)、`backend/`(Node 服务端 REST API + HTML 渲染)、`frontend/`(浏览器游戏/静态文件)、`container/`、`doc/`。构建：`haxelib install` + `yarn install` + `yarn run start`；可 `docker compose up --build` 直接跑。
- 注意区分：GitLab 上 `eternal-twin/eternal-twin` 仓库的 `alphabounce/` 只是原版 `ab_game.swf` 的**镜像存档**（无源码）；真正的重写在 `eternaltwin/alphabounce/alphabounce`。
- 已克隆到 `D:\Project\Self\EternalTwin-Alphabounce\`（约 62MB）。TODOLIST 显示仍在开发中（落地崩溃、部分砖块黑色、关卡结束弹窗等未完）。
