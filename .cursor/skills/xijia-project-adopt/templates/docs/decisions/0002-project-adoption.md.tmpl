# ADR 0002 - Project Adoption

- status: accepted
- date: {{date}}
- project: {{project_name}}
- owner: {{author}}

## 背景（Context）

历史多模块项目接入 xijia 工作流，需在不动业务代码的前提下建立文档基座与迭代就绪证据。

## 决策（Decision）

1. 使用 `/xijia:adopt` 而非 `/xijia:init` 作为主入口
2. 工作区 Git 拓扑：{{git_topology}}
3. 模块清单以 `docs/workspace-manifest.yaml` 为 SSOT
4. xijia-base 基座引用：{{xijia_base_ref}}
5. codegraph：{{codegraph_policy}}
6. DDD 必填：{{ddd_required}}

## 影响（Consequences）

- 日常入口切换为 `/xijia:start`（adopt verify 完成后）
- 多模块命令与 MCP 由 manifest 驱动
- 子项目原有 README/AGENTS 保留；根 `AGENTS.md` 为操作面板 SSOT
