---
标题: {{req_seq}}-后端工程初始化
状态: 待处理
负责人: {{author}}
创建时间: {{date}}
分级: 黄
类型: 技术
种子: true
openspec变更:
分级理由: 工程基线；引入依赖与目录约定，非核心业务规则
类型判型结论: 技术；后端工程壳层
DDD主类: D
Gate-0: 状态:complete；审批人:{{author}}；{{date}}
Gate-1: 状态:待批准；审批人:<git user.name>；{{date}}
Gate-2: 状态:待验收；审批人:<git user.name>；{{date}}
---

# {{req_seq}}-后端工程初始化

# Gate-0 澄清与范围

## 原始诉求（verbatim，禁止改写）

> `/xijia:init` 建立后端工程基线。

## 歧义登记

歧义登记: none（已逐句复核，触达面：后端工程初始化；确认人:{{author}}；日期:{{date}}）

## 业务目标

不适用（类型=技术）

## 用例 / 用户故事

不适用（类型=技术）

## 范围与切片

> 背景：`/xijia:init` 工程基线；代码根 `{{backend_path}}`；技术栈 {{chosen_stack_summary}}。
> In Scope 只写能力边界；可执行验收见 Gate-1 `## 验收标准`。

### In Scope（本期交付）

- 目录布局与入口可定位
- 依赖可安装；AGENTS 安装/启动命令可执行
- 健康检查或 hello 端点本地可验证
- 至少一条自动化测试或可运行验证命令
- AGENTS Build and test commands 已填写（非待补充）

### Out of Scope

- 不做业务功能、领域模型与业务 API（原因：本需求只建立工程壳层；重开触发：独立 PRD 需求进入 Gate-0）
- 不做前端工程（原因：由独立种子需求负责；重开触发：项目确认不拆分前后端）
- Gate-1 前不安装关键依赖或覆盖非空工程（原因：属于人工审批门禁；重开触发：Gate-1 已文字批准）

### 待确认问题与延期项（Open Questions & Deferred）

**Must-Confirm**

- OQ-001：采用的官方 CLI / 栈技能生成方式 → 结论：[待确认]

**Deferred**

- （无则写无）

## 约束引用

约束引用: none（已检索，触达面：工程基线冷启动；实现前按栈再检索 patterns/pitfalls）

## 数据流闭环表

本需求无业务数据流（工程基线）。验证以进程可启动与测试命令为准。

| 能力 | 来源(Source) | 加工(Process) | 去向(Sink) | 闭环 |
|---|---|---|---|---|
| 健康检查 | 本地请求/进程 | 框架路由直通 | 200/OK 响应 | 已确认 |

## 原型对齐与偏离

无原型对照（工程基线）。

# Gate-1 方案与验收

## 验收标准

- [ ] **AC-1**：目录布局符合确认栈惯例，入口文件可定位
  - **反例（本 AC 排除）**：仅有空目录无入口
- [ ] **AC-2**：依赖可安装；`AGENTS.md` 安装/启动命令可复制执行
  - **反例（本 AC 排除）**：命令仍为待补充
- [ ] **AC-3**：健康检查或 hello 端点（或等价）本地可验证
  - **反例（本 AC 排除）**：进程起不来或非 2xx
- [ ] **AC-4**：至少一条自动化测试或明确可运行验证命令通过
  - **反例（本 AC 排除）**：无测试且无验证命令
- [ ] **AC-5**：`AGENTS.md` **Build and test commands** 构建/测试命令已填写（非 `<待补充>`）
  - **反例（本 AC 排除）**：仍保留待补充占位

## 实现方案

- 复用映射 / 代码落点：`{{backend_path}}`；优先调用 init 已安装的后端栈技能或官方脚手架
- 切片拆解：

| 切片 | 覆盖 AC | 代码落点 | 验证命令 |
|---|---|---|---|
| 工程骨架 | AC-1, AC-2 | `{{backend_path}}` | `<安装/启动命令>` |
| 健康检查与测试 | AC-3, AC-4, AC-5 | `{{backend_path}}`、`AGENTS.md` | `<测试命令>` |

- 回归验证点：
  - `<AGENTS.md 中的测试/启动命令>`

# Gate-2 验收

## 验收记录

| AC | 结论 | 验证方式 | 结果摘要 |
|---|---|---|---|
| AC-1..5 | 通过/未过 | `<可执行命令>` | — |

- 变更文件：...
- 遗留 / 风险 / Deferred：none
- 一条可复跑验证命令：`<command>`

# Gate-3 沉淀

## 实现记录与沉淀

- Experience Reuse: none（工程冷启动）
- Capability Index: no-op
- Living Docs: updated | no-op
- Flow: updated | no-op
- Patterns: no-op
- Pitfalls: no-op
