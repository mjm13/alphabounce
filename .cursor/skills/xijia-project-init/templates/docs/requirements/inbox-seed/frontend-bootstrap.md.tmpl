---
标题: {{req_seq}}-前端工程初始化
状态: 待处理
负责人: {{author}}
创建时间: {{date}}
分级: 黄
类型: 技术
种子: true
openspec变更:
分级理由: 工程基线；引入依赖与目录约定，非核心业务规则
类型判型结论: 技术；前端工程壳层
DDD主类: D
Gate-0: 状态:complete；审批人:{{author}}；{{date}}
Gate-1: 状态:待批准；审批人:<git user.name>；{{date}}
Gate-2: 状态:待验收；审批人:<git user.name>；{{date}}
---

# {{req_seq}}-前端工程初始化

# Gate-0 澄清与范围

## 原始诉求（verbatim，禁止改写）

> `/xijia:init` 建立前端工程基线。

## 歧义登记

歧义登记: none（已逐句复核，触达面：前端工程初始化；确认人:{{author}}；日期:{{date}}）

## 业务目标

不适用（类型=技术）

## 用例 / 用户故事

不适用（类型=技术）

## 范围与切片

> 背景：`/xijia:init` 工程基线；代码根 `{{frontend_path}}`；技术栈 {{chosen_stack_summary}}。
> In Scope 只写能力边界；可执行验收见 Gate-1 `## 验收标准`。

### In Scope（本期交付）

- 目录布局与入口可定位
- 依赖可安装；dev 启动命令可执行
- 本地可打开默认页（或构建产物可预览）
- lint 或单测至少一种可运行验证
- AGENTS 前端相关命令已填写（非待补充）

### Out of Scope

- 不做业务页面、权限与真实业务 API 对接（原因：本需求只建立工程壳层；重开触发：独立 PRD 需求进入 Gate-0）
- 不做后端工程（原因：由独立种子需求负责；重开触发：项目确认前后端合并交付）
- Gate-1 前不覆盖非空前端工程（原因：属于人工审批门禁；重开触发：Gate-1 已文字批准）

### 待确认问题与延期项（Open Questions & Deferred）

**Must-Confirm**

- OQ-001：采用的官方 CLI / 栈技能生成方式 → 结论：[待确认]

**Deferred**

- （无则写无）

## 约束引用

约束引用: none（已检索，触达面：前端工程冷启动；实现前按栈再检索 UI patterns）

## 数据流闭环表

本需求无业务数据流（工程基线）。

| 能力 | 来源(Source) | 加工(Process) | 去向(Sink) | 闭环 |
|---|---|---|---|---|
| 默认页 | 本地静态/dev server | 框架直通 | 浏览器可访问 | 已确认 |

## 原型对齐与偏离

无原型对照（工程基线；业务页面对齐在后续 PRD 需求）。

# Gate-1 方案与验收

## 验收标准

- [ ] **AC-1**：目录布局符合确认栈惯例，入口可定位
  - **反例（本 AC 排除）**：仅有空目录无入口
- [ ] **AC-2**：依赖可安装；dev 启动命令可执行
  - **反例（本 AC 排除）**：命令仍为待补充
- [ ] **AC-3**：本地可打开默认页（或构建产物可预览）
  - **反例（本 AC 排除）**：dev/build 失败无法预览
- [ ] **AC-4**：lint 或单测至少一种可运行验证通过（按栈裁剪）
  - **反例（本 AC 排除）**：无 lint/单测命令
- [ ] **AC-5**：`AGENTS.md` 前端相关命令已填写（非 `<待补充>`）
  - **反例（本 AC 排除）**：仍保留待补充占位

## 实现方案

- 复用映射 / 代码落点：`{{frontend_path}}`；优先官方 CLI / 已装前端栈技能
- 切片拆解：

| 切片 | 覆盖 AC | 代码落点 | 验证命令 |
|---|---|---|---|
| 工程骨架与默认页 | AC-1, AC-2, AC-3 | `{{frontend_path}}` | `<安装/启动命令>` |
| 质量命令与文档 | AC-4, AC-5 | `{{frontend_path}}`、`AGENTS.md` | `<lint/测试/构建命令>` |

- 回归验证点：
  - `<AGENTS.md 中的前端测试/启动命令>`

# Gate-2 验收

## 验收记录

| AC | 结论 | 验证方式 | 结果摘要 |
|---|---|---|---|
| AC-1..5 | 通过/未过 | `<可执行命令>` | — |

- 变更文件：...
- 遗留 / 风险 / Deferred：none
- 一条可复跑验证命令：`<command>`

# Gate-3 沉淀

## 实现记录与沉淀

- Experience Reuse: none（工程冷启动）
- Capability Index: no-op
- Living Docs: updated | no-op
- Flow: updated | no-op
- Patterns: no-op
- Pitfalls: no-op
