# 归档区说明

本目录存放**已离场的过程产物**，默认不进入后续任务的 AI 上下文（见根目录 `.cursorignore`）。

## 适用内容

- spike / 探针报告（`spikes/`）
- 被替代或废弃的长文（按年份分子目录，如 `2026/`）
- 不宜删除但不再作为真相源的草稿

## 不适用

- 已发布领域语义 → `docs/domain/`
- 已交付需求 → `docs/requirements/shipped/`
- 已归档 OpenSpec change → `docs/openspec/changes/archive/`

## 使用方式

- 日常研发**不要**默认读取本目录
- 修历史 bug、追溯 AC 时，在对话中显式 `@docs/archive/...` 引用
