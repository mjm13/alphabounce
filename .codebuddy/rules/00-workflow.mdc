---
description: xijia 研发宪法（硬停 + guard 指针）；详细步骤见 xijia-ops-pipeline
alwaysApply: true
defends: "自主推进模式下 Agent 可凭程序证据自动推进并代签各 Gate（无需人工文字批准/签字）；但破坏性 DB/清库/新外部依赖/权限密钥变更/跨 BC 大规模调整仍须人工批准（22-db-destructive-safety / Approval Gates）。L3 责任边界，不到期。"
---

# 核心定位

- 真相源：测试 > spec；代码与已发布文档共同构成事实。
- 流程入口：优先 `/xijia:start`（`xijia-ops-pipeline`）；**空仓库**用 `/xijia:init`；**历史多模块**用 `/xijia:adopt`；单步命令见 `docs/process/project-lifecycle.md`。
- **详细执行步骤在技能中**；本文件只保留硬门禁与 guard 指针。
- 项目级硬约束：`docs/constitution.md`。

# 需求分级（摘要）

- Gate-0 先于分级：`已通过|部分通过|已驳回`（schema 见 `45-requirement-intake.mdc`；程序见 `.codebuddy/templates/requirements/gate0-intake.md`）。
- 分级矩阵与路由：`xijia-ops-pipeline/references/tier-routing.md`。
- frontmatter 须写 `分级`/`类型`（中文值）；`分级=红` 填 `openspec变更`。
- 🟢/🟡 Gate-1 前须有「验收标准 + 实现方案」：优先在 `/xijia:prd` 按分档写满（见 `.codebuddy/templates/requirements/gate1-by-tier.md`）；缺失由 A.0.5 + `writing-plans` 增量补全。红档须在 Gate-1 说明 OpenSpec 路径；无包时同黄档写满草案。
- 🔴：`explore → propose → analyze → apply → verify → sync → archive → Gate-2 → Gate-3`。
- 🧪 spike：链路不清先探针，代码非交付。

# 人审门禁（本项目：自主推进模式，已移除人工确认/签字）

> 项目决定（审批人 meijianming，2026-08-29）：**移除所有 Gate 的人工确认/签字环节**。Agent 依据文件证据与 guard 程序检查**自动推进并代签**，不等待人工文字批准或签字。以下仍强制保留（均由程序校验，非人工确认）：真机验收闭环、AC 断言、零 Godot ERROR、各 guard 检查、Approval Gates（破坏性操作）。
> 代签统一记 **审批人 = `agent-auto`**，日期取执行当日。

1. **Gate-0**：Agent 依据需求文档已结构化填写的 歧义登记/OQ/闭环表 直接判定 `已通过` 并回写（审批人 `agent-auto`）；无需人工逐条确认。`--check-intake` 通过即进 Gate-1。`部分通过`/`已驳回` 仅当 intake 程序硬 fail 时触发，届时 Agent 自行补 `gate0-intake` 闭环后重跑，不 solicite 人工。
2. **Gate-1**：Agent 直接批准 inbox 内已写满的 Gate-1（验收标准 + 实现方案），回写 `已批准`（审批人 `agent-auto`）；**批准后同轮进入实现**，不反问是否开工。
3. **Gate-2**：Agent 跑真机/组件 verify；证据（AC PASS + 零 ERROR + 截图，游戏/物理/交互/UI 须过**真机验收闭环**见 `42-verification-output.mdc` #11 与 `godot-android-debug` 技能）齐全即**自动签字** `已验收`（审批人 `agent-auto`），不得停轮等人工；`--check-release` 须在签字前执行；签字同轮进入 Gate-3。headless 单测/代码 review 不得冒充真机验收。
4. **Gate-3**：签字同轮 `xijia-sync-knowledge` → archive；任何档位强制；未完成不得开下一需求；`--check-release` 仍 blocking 时**在 Gate-3 链内修复后继续 Move**，不得停轮。

# Change 类型

`business|technical|hybrid|defect`（业务|技术|混合|缺陷）；technical/defect 不得虚构 DDD；technical 用 `.codebuddy/templates/requirements/technical-requirement-template.md`，defect 用 `.codebuddy/templates/requirements/defect-template.md`；权衡写 ADR 不写 domain。

# 文档边界（摘要）

活文档 vs 过程文档 / 经验复用 / DDD 分类：见 `xijia-ops-pipeline/references/living-docs.md`。

# 收尾（9 步，不得跳过）

verify → comment-sync → 验收记录 → `--check-release` → Gate-2 → Gate-3 → `--check-closeout` → commit（用户触发）→ Deferred 入 backlog。详见 `xijia-ops-pipeline/references/closeout-steps.md`。

Gate-3 归档（`xijia-sync-knowledge` 步骤 17.5–18）：inbox **改状态 → 写总结 → Move-Item**；**禁止** inbox 不可见时 rebuild shipped；**禁止**对 `docs/requirements/shipped/` 用 Cursor Write（见 `xijia-safe-file-write`）。

# Approval Gates（命中即停）

破坏性 DB、清库/重置（`22-db-destructive-safety.mdc`）、新外部依赖、下线能力、权限/密钥变更、跨 BC 大规模调整。

# Guard 命令

可执行 guard / 提交约定见根目录 `AGENTS.md`（Commit and PR instructions）。

# 关联规则（按需）

`40-evidence-chain` `41-change-boundary` `42-verification-output` `43-correction-learning` `44-comment-sync` `45-requirement-intake` `46-git-branching` `47-release-lifecycle` `07-xijia-skill-naming` `22-db-destructive-safety`
