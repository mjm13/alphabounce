# 能力追溯索引（Capability Map）

> 行主键：`moduleKey` + `前端入口`（normalize 后）。Gate-3 **动态合并**（ADD/UPDATE/DEPRECATE），禁止同主键重复行。
> 操作级细节真相源：各 `docs/requirements/shipped/*` 需求「数据流闭环表」。
> 维护：`xijia-sync-knowledge` Gate-3 调用 `python .codebuddy/hooks/extract_capability_index.py --req <shipped-req>`。

| 模块 | moduleKey | 前端入口 | 后端能力 | 相关表 | 来源摘要 | 去向摘要 | 状态 | 需求来源 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| {{primary_modules}} | <module-key> | <route-or-menu> | <api/service> | <table> | <source-summary> | <sink-summary> | active | <shipped-req> |

## 修订记录

| 日期 | 操作 | 主键 | 需求来源 | 说明 |
| --- | --- | --- | --- | --- |
| {{date}} | ADD | <module-key\|frontend> | <shipped-req> | init placeholder |
